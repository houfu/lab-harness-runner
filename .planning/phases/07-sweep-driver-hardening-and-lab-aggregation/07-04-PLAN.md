---
phase: 07-sweep-driver-hardening-and-lab-aggregation
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md
  - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REPLAY.md
autonomous: true
requirements: [SWP-01, SWP-02]
must_haves:
  truths:
    - "A REVIEW.md captures what each of the four post-v1.0 sweep.sh commits contributed and what v1.1 builds on"
    - "A REPLAY.md records the inventory output against the existing harvey-labs/results/ data with actual counts"
    - "The replay figures match the live on-disk metrics (170 total, 136 clean, 34 timeout) and reconcile the ROADMAP's stale 174 figure"
  artifacts:
    - path: ".planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md"
      provides: "Code review of commits 3a1fd89, 17d3eb7, 3e0dd71, 2884ae7"
      contains: "3a1fd89"
    - path: ".planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REPLAY.md"
      provides: "Replay analysis of hardened inventory against live results"
      contains: "incomplete"
  key_links:
    - from: "REPLAY.md figures"
      to: "harvey-labs/results on-disk metrics.json"
      via: "bash scripts/sweep.sh inventory over live data"
      pattern: "clean=\\|clean:"
---

<objective>
Produce two planning artifacts that close the phase's review-and-verification deliverables:
(1) `REVIEW.md` — a code review of the four post-v1.0 `sweep.sh` commits (`3a1fd89`, `17d3eb7`,
`3e0dd71`, `2884ae7`), recording what each contributed and what v1.1 hardening builds on top of
each; (2) `REPLAY.md` — a replay analysis running the hardened `inventory` (from Plan 01) against
the existing `~/Projects/harvey-labs/results/` data, recording actual counts and reconciling the
ROADMAP's stale "174" figure against the live 170 (136 clean + 34 timeout).

Purpose: These are the ROADMAP-mandated verification deliverables for Phase 7 (code review of the
substrate commits + replay against real data — no new live sweep required). They confirm the
hardened driver behaves correctly against the data set that exercises every code path.

Output: `REVIEW.md` and `REPLAY.md` in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-CONTEXT.md
@.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-RESEARCH.md
@.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write REVIEW.md — code review of the four post-v1.0 sweep.sh commits</name>
  <files>.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md</files>
  <read_first>
    - scripts/sweep.sh (current full file — the substrate the four commits produced)
    - .planning/ROADMAP.md (Phase 7 Context section — the "four post-v1.0 commits" descriptions and the State of the Art table in RESEARCH)
    - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-RESEARCH.md (State of the Art table mapping each commit to its change)
  </read_first>
  <action>
    Run `git log --oneline --stat 3a1fd89 17d3eb7 3e0dd71 2884ae7 -n 1` (or `git show --stat <sha>` per commit)
    to confirm each commit's message, date, and touched lines, then write `REVIEW.md`. Structure: one section
    per commit (heading `### <sha> — <subject>`), each recording: what the commit changed in sweep.sh, why
    (the problem it solved), and what v1.1 Phase 7 builds on top of it. Anchor each to the RESEARCH State of
    the Art table: `3a1fd89` = deterministic run-ids + xargs -P parallelism + skip-on-clean + FAILED/MISSING
    inventory; `17d3eb7` = venv-direct python (ENFILE fix at ~193 tasks) + run_one always returns 0;
    `3e0dd71` and `2884ae7` = confirm their actual subjects from git and describe (TIMEOUT env var / destroy-shim
    stderr surfacing per ROADMAP context). Add a final "What v1.1 hardens" subsection mapping each Phase 7 change
    (TIMEOUT doc, dual inventory, summary line, exit-code, LAB_COMPARE) to the commit(s) it extends. Use the
    verified git facts, not assumptions — if a commit's subject differs from the ROADMAP description, record the
    actual subject and note the discrepancy.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md && grep -q '3a1fd89' .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md && grep -q '17d3eb7' .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md && grep -q '3e0dd71' .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md && grep -q '2884ae7' .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/.../REVIEW.md` exists
    - All four shas (`3a1fd89`, `17d3eb7`, `3e0dd71`, `2884ae7`) each appear in the document
    - Each commit section states what it changed AND what v1.1 builds on it
    - Commit subjects/dates match `git show --stat <sha>` output (verified, not assumed)
    - A "What v1.1 hardens" subsection maps Phase 7 changes to the underlying commits
  </acceptance_criteria>
  <done>REVIEW.md documents all four commits with verified git facts, the problem each solved, and the v1.1 build-on mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Write REPLAY.md — replay analysis of hardened inventory against live results</name>
  <files>.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REPLAY.md</files>
  <read_first>
    - scripts/sweep.sh (the hardened `inventory()` from Plan 01 — the function being replayed)
    - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-RESEARCH.md (Pitfall 1 — live data 170 total / 136 clean / 34 timeout vs ROADMAP 174; Open Question 2)
    - .planning/ROADMAP.md (Phase 7 exit criterion stating 140 clean + 34 timeout = 174)
  </read_first>
  <action>
    Run the hardened inventory against live data and capture the result, then write `REPLAY.md`. Commands to run:
    (a) count live metrics with
    `find "$HOME/Projects/harvey-labs/results" -maxdepth 2 -name metrics.json | wc -l` (expect 170);
    (b) count clean: `grep -rl '"benchmark_status": *"clean"' "$HOME/Projects/harvey-labs/results"/*/metrics.json | wc -l` (expect 136);
    (c) count timeout: `grep -rl '"benchmark_status": *"timeout"' "$HOME/Projects/harvey-labs/results"/*/metrics.json | wc -l` (expect 34);
    (d) run `bash scripts/sweep.sh inventory` and capture its `total:` / `clean:` / `incomplete:` header lines and
    confirm the bare path lines after `---` are consumable (pipe `bash scripts/sweep.sh inventory | tail -n +5 | head` to
    show bare paths). Note: the inventory `incomplete` count is over the FULL task list (tasks dir), which may exceed
    the 170 result dirs because tasks without any result dir are also incomplete — record the actual numbers the
    command prints and explain the relationship rather than forcing a match. Write REPLAY.md recording: the live
    metrics counts (total/clean/timeout), the inventory header output, a confirmation that path lines carry no
    `FAILED/MISSING:` prefix and are xargs-consumable, and an explicit reconciliation of the ROADMAP's stale figure
    (174 / 140 clean) against the live figure (170 / 136 clean) — the 4-dir discrepancy is stale ROADMAP data per
    RESEARCH Pitfall 1. State the corrected exit-criterion figures.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REPLAY.md && grep -q 'incomplete' .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REPLAY.md && grep -Eq '170|136' .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/REPLAY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/.../REPLAY.md` exists
    - Records the live metrics counts (total=170, clean=136, timeout=34) as actually measured
    - Includes the captured `bash scripts/sweep.sh inventory` header (`total:`/`clean:`/`incomplete:`) output
    - Confirms inventory path lines carry no `FAILED/MISSING:` prefix and are xargs-consumable
    - Reconciles the ROADMAP stale figure (174 / 140 clean) against the live 170 / 136 clean, citing RESEARCH Pitfall 1
    - Figures in the document are produced by re-running the commands, not copied blindly (executor runs them in this task)
  </acceptance_criteria>
  <done>REPLAY.md records the live inventory replay with actual counts, confirms CI-consumable bare path output, and reconciles the stale ROADMAP figure against the corrected live figure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live results tree → replay commands | Read-only inspection of on-disk metrics.json |
| git history → REVIEW.md | Read-only inspection of commit metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Repudiation | inaccurate replay figures copied from stale ROADMAP | mitigate | Executor re-runs the count commands and records measured values; reconciliation section flags the stale 174 figure explicitly |
| T-07-13 | Tampering | none — both artifacts are read-only analyses | accept | No code or live system is modified; documents only |
</threat_model>

<verification>
- Both `REVIEW.md` and `REPLAY.md` exist in the phase directory
- REVIEW.md contains all four commit shas with verified subjects
- REPLAY.md contains the live counts (170/136/34) and the stale-figure reconciliation
</verification>

<success_criteria>
- Code review deliverable: REVIEW.md documents the four substrate commits and the v1.1 build-on mapping
- Replay deliverable: REPLAY.md confirms hardened inventory against live data, records actual counts, reconciles the stale ROADMAP figure
- No live sweep was run — analysis uses existing results data per the phase's verification posture
</success_criteria>

<output>
Create `.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-04-SUMMARY.md` when done
</output>
