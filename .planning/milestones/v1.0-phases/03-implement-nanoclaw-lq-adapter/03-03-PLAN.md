---
phase: 03-implement-nanoclaw-lq-adapter
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - results/
autonomous: false
requirements: [REQ-MOUNTS, REQ-DISPATCH, REQ-EXIT]
user_setup:
  - service: nanoclaw mount-allowlist
    why: "additional_mounts are silently dropped unless the Harvey LAB root is in allowedRoots (currently empty)"
    dashboard_config:
      - task: "Add /Users/houfu/Projects/harvey-labs to allowedRoots (allowReadWrite: true)"
        location: "~/.config/nanoclaw/mount-allowlist.json"
  - service: nanoclaw LAB agent group
    why: "ncl groups create requires interactive approval; the _ping-test group uses Ollama (blocks api.anthropic.com), unusable for LAB legal drafting"
    dashboard_config:
      - task: "Create/confirm a nanoclaw agent group configured for the Anthropic Claude provider for LAB runs; record its group id"
        location: "nanoclaw-lq daemon: ncl groups create (approval) + container config"
must_haves:
  truths:
    - "The Harvey LAB root is present in mount-allowlist.json allowedRoots so additional mounts are not silently dropped"
    - "A nanoclaw agent group configured for Anthropic Claude exists and its group id is known"
    - "Running the proof task end to end produces results/<run-id>/output/discrepancy-analysis-memo.docx (non-zero bytes)"
    - "metrics.json is written with the recorded end_state for the run"
  artifacts:
    - path: "results"
      provides: "the run output tree produced by the proof run (results/<run-id>/output/)"
  key_links:
    - from: "scripts/nanoclaw_run.py"
      to: "results/<run-id>/output/discrepancy-analysis-memo.docx"
      via: "NanoclawAdapter.run dispatch -> agent writes deliverable -> lab-output mount"
      pattern: "discrepancy-analysis-memo.docx"
---

<objective>
Close the exit criterion: with the Node shim + adapter from Plan 02, perform the
one-time human setup (mount-allowlist + Anthropic-Claude LAB group) and run the
proof task `corporate-ma/compare-matter-plan-against-engagement-letter` end to
end through nanoclaw-lq, confirming the expected deliverable lands in
`results/<run-id>/output/`.

Purpose: Phase 3's exit criterion is "one LAB task reaches nanoclaw and produces
at least one expected deliverable in results/<run-id>/output/." This plan provides
the human-only setup nanoclaw requires (research Pitfalls 1 and 6) and the smoke
run that proves the full path.

Output: a populated mount-allowlist, a confirmed LAB group id, and a real
`results/<run-id>/output/discrepancy-analysis-memo.docx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-implement-nanoclaw-lq-adapter/03-RESEARCH.md
@scripts/nanoclaw_run.py
@lab_harness_runner/nanoclaw_adapter.py

<interfaces>
Proof task (RESEARCH Single-Task Proof):
- task_id: corporate-ma/compare-matter-plan-against-engagement-letter
- documents: engagement-letter.docx, matter-plan.docx
- expected deliverable: discrepancy-analysis-memo.docx (single file)
- documents dir: /Users/houfu/Projects/harvey-labs/tasks/corporate-ma/compare-matter-plan-against-engagement-letter/documents/

Required mount-allowlist.json allowedRoots entry (RESEARCH Pitfall 1):
  { "path": "/Users/houfu/Projects/harvey-labs", "allowReadWrite": true,
    "description": "Harvey LAB tasks and results (lab-harness-runner)" }

Run command (RESEARCH Phase gate):
  uv run python scripts/nanoclaw_run.py \
    --task corporate-ma/compare-matter-plan-against-engagement-letter \
    --nanoclaw-dir /Users/houfu/Projects/nanoclaw-lq --group-id <LAB group id>
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plan 02 delivered the dispatch shim and adapter. nanoclaw requires two pieces of state that cannot be created non-interactively: the mount allowlist (silently drops mounts when empty — Pitfall 1) and an Anthropic-Claude agent group (`ncl groups create` needs human approval — Pitfall 6; the existing `_ping-test` group uses Ollama and blocks api.anthropic.com).</what-built>
  <how-to-verify>
    1. Edit `~/.config/nanoclaw/mount-allowlist.json`: add to allowedRoots the entry
       { "path": "/Users/houfu/Projects/harvey-labs", "allowReadWrite": true,
         "description": "Harvey LAB tasks and results (lab-harness-runner)" }.
       Confirm `allowedRoots` is no longer empty.
    2. Ensure a nanoclaw agent group exists that is configured for the Anthropic
       Claude provider (NOT Ollama, NOT blocking api.anthropic.com). Either create
       one via `ncl groups create` (approve the prompt) or confirm an existing
       Claude group. Record its group id.
    3. Confirm the nanoclaw daemon is running and Docker is up
       (e.g. `data/ncl.sock` present; `docker ps` works).
    4. Report the chosen LAB group id here — it is the `--group-id` value for the run.
  </how-to-verify>
  <resume-signal>Reply with the LAB group id and confirm allowlist + daemon + Docker are ready (e.g. "group-id=lab-runner, allowlist updated, daemon+docker up").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the proof run end to end</name>
  <files>results/</files>
  <read_first>
    - scripts/nanoclaw_run.py (CLI args and wiring)
    - lab_harness_runner/nanoclaw_adapter.py (run() — mounts, dispatch, poll)
    - .planning/phases/03-implement-nanoclaw-lq-adapter/03-RESEARCH.md (Single-Task Proof; Pitfalls 4,5; timeout 600s)
  </read_first>
  <action>
    Run the proof task through the adapter using the group id from Task 1:
    `uv run python scripts/nanoclaw_run.py --task
    corporate-ma/compare-matter-plan-against-engagement-letter --nanoclaw-dir
    /Users/houfu/Projects/nanoclaw-lq --group-id &lt;LAB group id&gt;` (default
    timeout 600s). Capture the printed run id and run directory. The adapter
    configures the document RO + output RW mounts, dispatches the inbound message
    (instructions + footer), wakes the container, and polls outbound.db for STATUS:.
    If the run reports `timeout` or `agent_error`, capture the run id, the
    metrics.json end_state, and any container/daemon logs, and surface the failure
    (do NOT fake or hand-place the deliverable — that would violate the exit
    criterion's intent). On a clean run, note the run-id for the verify checkpoint.
  </action>
  <verify>
    <automated>test -n "$(find /Users/houfu/Projects/harvey-labs/results -path '*/output/discrepancy-analysis-memo.docx' -size +0c -print -quit 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/nanoclaw_run.py` ran against the proof task and printed a run id + run directory.
    - A file `results/<run-id>/output/discrepancy-analysis-memo.docx` exists with non-zero size under the Harvey LAB results tree.
    - `metrics.json` exists in the run dir with an `end_state` field (recorded distinctly: clean / agent_error / timeout).
    - If end_state != "clean", the failure (id + end_state + logs) is reported rather than worked around.
  </acceptance_criteria>
  <done>The proof run produced a non-zero discrepancy-analysis-memo.docx in results/<run-id>/output/ and a metrics.json recording the end_state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>An end-to-end nanoclaw-lq run of the proof task that should have produced results/&lt;run-id&gt;/output/discrepancy-analysis-memo.docx and a metrics.json with the recorded end_state.</what-built>
  <how-to-verify>
    1. Confirm the file exists and is non-empty:
       `ls -la /Users/houfu/Projects/harvey-labs/results/&lt;run-id&gt;/output/discrepancy-analysis-memo.docx`
    2. Open the .docx and confirm it contains real generated analysis content
       (not a placeholder / empty document).
    3. Inspect `results/&lt;run-id&gt;/metrics.json`: confirm `end_state` is "clean"
       and `wall_clock_seconds` is recorded.
    4. (Optional) Confirm the container actually saw the mounts: documents were
       read from /workspace/extra/lab-documents and the deliverable was written via
       /workspace/extra/lab-output (REQ-MOUNTS, REQ-DISPATCH evidence).
  </how-to-verify>
  <resume-signal>Type "approved" if the deliverable exists with real content and end_state is clean; otherwise describe the failure (timeout, empty doc, missing mounts).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → mount-allowlist.json | the allowlist root becomes a valid Docker bind-mount source for the LAB tree |
| nanoclaw container → Harvey LAB results dir (RW mount) | the agent writes deliverable files into the host results tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Elevation of Privilege | mount-allowlist allowedRoots scope | mitigate | Allowlist root is scoped to `/Users/houfu/Projects/harvey-labs` only (the LAB tree), not `/` or a home dir; allowReadWrite limited to that root |
| T-03-09 | Tampering | RW output mount the agent can write to | accept | The agent (Claude in a sandboxed container) writes only under the run's output dir; the mount is the run's own results/<run-id>/output; same-machine, single-task proof, low risk |
| T-03-10 | Spoofing | reused LAB group session accumulating prior context | accept | agent-shared session for the single-task proof per RESEARCH Q3; multi-run isolation is a Phase 4 concern |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- Exit criterion (manual smoke, daemon + Docker + Anthropic model): the proof run
  produces `results/<run-id>/output/discrepancy-analysis-memo.docx` (non-zero) and
  a `metrics.json` recording end_state.
- mount-allowlist.json allowedRoots includes the Harvey LAB root.
- A Claude-configured LAB group id is recorded and used as --group-id.
</verification>

<success_criteria>
- The one-time human setup (allowlist + Claude group) is complete and recorded.
- One real LAB task reached nanoclaw and produced the expected deliverable in
  results/<run-id>/output/ — the Phase 3 exit criterion is met.
- The run's end_state is recorded in metrics.json (clean / agent_error / timeout
  distinctly).
</success_criteria>

<output>
Create `.planning/phases/03-implement-nanoclaw-lq-adapter/03-03-SUMMARY.md` when done.
</output>
