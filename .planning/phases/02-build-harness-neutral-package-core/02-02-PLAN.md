---
phase: "02-build-harness-neutral-package-core"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lab_harness_runner/task_reader.py
  - lab_harness_runner/result_builder.py
autonomous: true
requirements:
  - Read LAB task metadata from task.json
  - Support task instructions stored inline in task.json
  - Extract expected deliverable filenames from criteria[].deliverables
  - Build a TaskSpec containing instructions, documents directory, expected deliverables, and run id
  - Create results/<run-id>/output/ for each run
must_haves:
  truths:
    - "read_task('lab_path', 'area/slug', 'run-id') returns a TaskSpec with correct fields"
    - "Deliverables are extracted from criteria[].deliverables, not top-level deliverables dict"
    - "task_id with path traversal chars raises ValueError before any filesystem access"
    - "build_result_dir creates lab_path/results/<run-id>/output/ on disk"
    - "build_result_dir returns (run_dir, output_dir) as a tuple[Path, Path]"
  artifacts:
    - path: "lab_harness_runner/task_reader.py"
      provides: "read_task function"
      exports: ["read_task"]
    - path: "lab_harness_runner/result_builder.py"
      provides: "build_result_dir function"
      exports: ["build_result_dir"]
  key_links:
    - from: "lab_harness_runner/task_reader.py"
      to: "lab_path/tasks/<task-id>/task.json"
      via: "json.loads + Path read"
      pattern: "task_json\\.read_text"
    - from: "lab_harness_runner/result_builder.py"
      to: "lab_path/results/<run-id>/output/"
      via: "Path.mkdir(parents=True, exist_ok=True)"
      pattern: "output_dir\\.mkdir"
---

<objective>
Implement the two file-I/O modules: task_reader.py (reads task.json and returns TaskSpec)
and result_builder.py (creates the run directory tree and returns paths).

Purpose: These are the first two steps in every run. task_reader extracts the contracts
the adapter must fulfill. result_builder creates the filesystem location where the adapter
writes deliverables.

Output: read_task and build_result_dir functions, importable from lab_harness_runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-build-harness-neutral-package-core/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from adapter.py (Wave 1) that these modules must use -->

From lab_harness_runner/adapter.py:
  @dataclass TaskSpec:
    task_id: str
    instructions: str
    documents_dir: Path
    expected_deliverables: list[str]
    run_id: str

Function signatures to implement:

task_reader.py:
  def read_task(lab_path: Path, task_id: str, run_id: str) -> TaskSpec
    - Raises FileNotFoundError if task.json not found
    - Raises ValueError if task_id contains ".." or absolute path
    - Raises KeyError if "instructions" missing from task.json
    - Extracts deliverables from criteria[].deliverables (NOT top-level "deliverables" dict)
    - Sets documents_dir = lab_path / "tasks" / <task_id_as_path> / "documents"

  def _lab_path(override: Path | None = None) -> Path
    - Returns override if provided
    - Returns Path(os.environ["HARVEY_LAB_PATH"]) if env var set (D-08)
    - Returns Path.home() / "Projects" / "harvey-labs" as fallback (D-08)

result_builder.py:
  def build_result_dir(lab_path: Path, run_id: str) -> tuple[Path, Path]
    - run_dir = lab_path / "results" / run_id
    - output_dir = run_dir / "output"
    - Creates output_dir with mkdir(parents=True, exist_ok=True)
    - Returns (run_dir, output_dir)

LAB task.json verified schema (from docs/verified-contracts.md):
  Required fields: title, instructions, criteria
  criteria[].deliverables: list[str] — per-criterion expected filenames
  IMPORTANT: top-level "deliverables" key is a dict — do NOT use it for expected filenames

Path safety guard (from scripts/lab_probe.py lines 13-19):
  _reject_unsafe_relative_path(value: str, name: str) -> Path
    path = Path(value)
    if path.is_absolute(): raise ValueError(...)
    if any(part in {"", ".", ".."} for part in path.parts): raise ValueError(...)
    return path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement task_reader.py</name>
  <files>lab_harness_runner/task_reader.py</files>
  <read_first>
    - scripts/lab_probe.py — load_task (lines 22-26), expected_deliverables (lines 29-36), reject_unsafe_relative_path (lines 13-19)
    - .planning/phases/02-build-harness-neutral-package-core/02-PATTERNS.md — task_reader.py section
    - .planning/phases/02-build-harness-neutral-package-core/02-CONTEXT.md — D-04 (TaskSpec fields), D-08 (lab path resolution)
    - docs/verified-contracts.md — Task Schema section (criteria[].deliverables vs top-level deliverables)
    - lab_harness_runner/adapter.py — TaskSpec import
  </read_first>
  <behavior>
    - read_task with valid task_id returns TaskSpec with task_id, instructions, run_id set from args and documents_dir = lab_path/"tasks"/<task_path>/"documents"
    - read_task extracts deliverables only from criteria[].deliverables — not from top-level "deliverables" key
    - read_task with task_id containing ".." raises ValueError before any filesystem read
    - read_task with task_id as absolute path raises ValueError
    - read_task when task.json is missing raises FileNotFoundError
    - _lab_path() with no args returns Path.home() / "Projects" / "harvey-labs"
    - _lab_path() when HARVEY_LAB_PATH env var set returns Path(env_value)
    - _lab_path(override=some_path) returns some_path
  </behavior>
  <action>
    Create lab_harness_runner/task_reader.py:

    1. File header: from __future__ import annotations
    2. Imports: json, os from stdlib; Path from pathlib; TYPE_CHECKING guard not needed since TaskSpec is used at runtime
    3. Import TaskSpec from lab_harness_runner.adapter
    4. Implement module-private _reject_unsafe_relative_path(value: str, name: str) -> Path — exact pattern from lab_probe.py lines 13-19; raises ValueError for absolute paths or segments in {"", ".", ".."}
    5. Implement module-private _lab_path(override: Path | None = None) -> Path — per D-08: returns override if not None, else os.environ.get("HARVEY_LAB_PATH") as Path, else Path.home() / "Projects" / "harvey-labs"
    6. Implement read_task(lab_path: Path, task_id: str, run_id: str) -> TaskSpec:
       - Call _reject_unsafe_relative_path(task_id, "task_id") to get task_path: Path
       - task_dir = lab_path / "tasks" / task_path
       - task_json = task_dir / "task.json"
       - Raise FileNotFoundError if task_json does not exist (include full path in message)
       - config = json.loads(task_json.read_text(encoding="utf-8"))
       - Extract instructions = config["instructions"] — let KeyError propagate (do not silently fall back to instructions.md per verified-contracts.md)
       - Extract deliverables: iterate criteria[], collect deliverable strings into a set, return sorted list (pattern from lab_probe.py expected_deliverables)
       - Raise ValueError if any deliverable value is not a str
       - Return TaskSpec(task_id=task_id, instructions=instructions, documents_dir=task_dir/"documents", expected_deliverables=sorted_deliverables, run_id=run_id)
    7. Run `uv run black lab_harness_runner/task_reader.py`
  </action>
  <verify>
    <automated>uv run pytest tests/test_task_reader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - lab_harness_runner/task_reader.py exists
    - `from lab_harness_runner.task_reader import read_task` succeeds
    - `from lab_harness_runner import read_task` succeeds (re-exported via __init__.py)
    - pytest tests/test_task_reader.py exits 0 with all tests passing (tests created in plan 04)
    - task_reader.py reads from criteria[].deliverables — confirmed by grep: `grep -v '^#' lab_harness_runner/task_reader.py | grep 'criteria'` returns at least one match
    - task_reader.py does NOT read top-level "deliverables" key — confirmed by grep: `grep -v 'criteria' lab_harness_runner/task_reader.py | grep '"deliverables"'` returns empty
    - `uv run black --check lab_harness_runner/task_reader.py` exits 0
  </acceptance_criteria>
  <done>read_task is implemented and importable. It correctly reads task.json, extracts deliverables from criteria[].deliverables, validates task_id path safety, and returns a TaskSpec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement result_builder.py</name>
  <files>lab_harness_runner/result_builder.py</files>
  <read_first>
    - scripts/lab_probe.py — directory creation pattern (lines 126-128)
    - .planning/phases/02-build-harness-neutral-package-core/02-PATTERNS.md — result_builder.py section
    - .planning/phases/02-build-harness-neutral-package-core/02-CONTEXT.md — D-09 (results directory layout)
    - docs/verified-contracts.md — Result Layout section
    - lab_harness_runner/adapter.py — (no import needed from adapter; returns plain Paths)
  </read_first>
  <behavior>
    - build_result_dir(lab_path, "my-run") creates lab_path/results/my-run/output/ on disk
    - build_result_dir returns a tuple where first element is run_dir and second is output_dir
    - build_result_dir(lab_path, "my-run").run_dir == lab_path / "results" / "my-run"
    - build_result_dir(lab_path, "my-run").output_dir == lab_path / "results" / "my-run" / "output"
    - build_result_dir called twice with same run_id does not raise (exist_ok=True)
    - build_result_dir with run_id containing no slashes creates a flat directory
  </behavior>
  <action>
    Create lab_harness_runner/result_builder.py:

    1. File header: from __future__ import annotations
    2. Imports: Path from pathlib only (no other imports needed)
    3. Implement build_result_dir(lab_path: Path, run_id: str) -> tuple[Path, Path]:
       - run_dir = lab_path / "results" / run_id — Python Path handles slash-containing run_id as nested directories automatically (no split needed per PATTERNS.md)
       - output_dir = run_dir / "output"
       - output_dir.mkdir(parents=True, exist_ok=True)
       - return run_dir, output_dir
    4. Run `uv run black lab_harness_runner/result_builder.py`
  </action>
  <verify>
    <automated>uv run pytest tests/test_result_builder.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - lab_harness_runner/result_builder.py exists
    - `from lab_harness_runner.result_builder import build_result_dir` succeeds
    - `from lab_harness_runner import build_result_dir` succeeds (re-exported via __init__.py)
    - pytest tests/test_result_builder.py exits 0
    - result_builder.py contains `output_dir.mkdir(parents=True, exist_ok=True)`
    - `uv run black --check lab_harness_runner/result_builder.py` exits 0
  </acceptance_criteria>
  <done>build_result_dir creates the run directory tree on disk and returns (run_dir, output_dir). The function is importable from lab_harness_runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task_id string → lab_path/tasks/<task_id> | External string value becomes a filesystem path component |
| run_id string → lab_path/results/<run_id> | External string value becomes a filesystem path component |
| HARVEY_LAB_PATH env var → Path | Environment variable becomes a filesystem root path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | task_id → path traversal | mitigate | _reject_unsafe_relative_path validates no "..", no absolute prefix, no empty segments before any filesystem access |
| T-02-04 | Tampering | run_id → path traversal | accept | run_id in this phase always comes from uuid.uuid4() in fake_run.py; caller is trusted code; full validation deferred to Phase 3 adapter layer where run_id originates from external input |
| T-02-05 | Information Disclosure | HARVEY_LAB_PATH env var | accept | Lab path is a local development path, not a secret; disclosure risk is negligible in local-only tool context |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (stdlib only); uv_build/black/pytest installed in Wave 1 plan 01 |
</threat_model>

<verification>
- `from lab_harness_runner import read_task, build_result_dir` exits 0
- `uv run pytest tests/test_task_reader.py tests/test_result_builder.py -x -q` exits 0 (after tests created in plan 04)
- Both modules are black-formatted
- task_reader.py reads criteria[].deliverables, not top-level "deliverables" dict
</verification>

<success_criteria>
read_task and build_result_dir are fully implemented, importable from lab_harness_runner, and covered by unit tests. The two harness-agnostic I/O operations (task reading and directory creation) are complete.
</success_criteria>

<output>
Create `.planning/phases/02-build-harness-neutral-package-core/02-02-SUMMARY.md` when done.
</output>
