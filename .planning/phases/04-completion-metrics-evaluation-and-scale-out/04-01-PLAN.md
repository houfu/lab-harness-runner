---
id: 04-01
phase: 04-completion-metrics-evaluation-and-scale-out
plan: 01
title: Benchmark status semantics and metrics diagnostics
type: execute
status: ready
wave: 1
depends_on: []
files_modified:
  - lab_harness_runner/status.py
  - lab_harness_runner/metrics.py
  - lab_harness_runner/__init__.py
  - tests/test_status.py
  - tests/test_metrics.py
autonomous: true
requirements: [REQ-09, REQ-21]
requirements_addressed: [REQ-09, REQ-21]
source_decisions: [D-01, D-02, D-03, D-07, D-09]
read_first:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
  - .planning/phases/03-implement-nanoclaw-lq-adapter/03-03-SUMMARY.md
  - .planning/phases/03-implement-nanoclaw-lq-adapter/03-UAT.md
  - lab_harness_runner/adapter.py
  - lab_harness_runner/metrics.py
  - lab_harness_runner/evaluator.py
acceptance_criteria:
  - A run with all expected deliverables present and raw adapter end_state timeout derives benchmark_status clean while preserving raw_end_state timeout, per D-01/D-02/D-03.
  - A run with missing expected deliverables derives benchmark_status error or timeout based on raw state and records expected_deliverables_present false plus missing_deliverables.
  - metrics.json keeps existing LAB metric keys and adds diagnostic fields without null values.
  - Whole agent-system metadata fields include adapter, task_id, run_id, run_dir, output_dir, and status diagnostics; no code writes outside /Users/houfu/Projects/harvey-labs/results/<run-id>/ for per-run artifacts.
tests:
  - uv run pytest tests/test_status.py tests/test_metrics.py -q
  - uv run pytest tests/ -q
threat_model:
  trust_boundaries:
    - output_dir filesystem state -> benchmark status derivation
    - adapter RunResult -> metrics/reporting layer
  stride:
    - id: T-04-01-R
      category: Repudiation
      component: benchmark status derivation
      disposition: mitigate
      mitigation: Preserve raw_end_state, terminal_status_seen, completion_signal, expected_deliverables_present, and missing_deliverables separately from benchmark_status.
    - id: T-04-01-T
      category: Tampering
      component: deliverable validation paths
      disposition: mitigate
      mitigation: Validate only filenames from TaskSpec.expected_deliverables under the known output_dir; do not accept absolute or traversal deliverable paths.
must_haves:
  truths:
    - Valid deliverables can be benchmark-clean even when STATUS:DONE was not observed.
    - Raw adapter/protocol state remains visible in metrics and later aggregate rows.
    - Metrics remain LAB-compatible and continue to provide safe defaults.
  artifacts:
    - path: lab_harness_runner/status.py
      provides: Benchmark status derivation and deliverable validation diagnostics.
      contains: derive_benchmark_status
    - path: lab_harness_runner/metrics.py
      provides: metrics.json writer with optional diagnostic fields.
      contains: write_metrics
    - path: tests/test_status.py
      provides: Unit coverage for mixed timeout-with-valid-output semantics.
      contains: test_timeout_with_valid_deliverables_is_benchmark_clean
  key_links:
    - from: lab_harness_runner/status.py
      to: lab_harness_runner.adapter.RunResult
      via: raw end_state input
      pattern: raw_end_state
    - from: lab_harness_runner/metrics.py
      to: /Users/houfu/Projects/harvey-labs/results/<run-id>/metrics.json
      via: write_metrics output
      pattern: benchmark_status
---

<objective>
Create the benchmark-facing status layer required for honest Phase 4 reporting.

Purpose: Phase 3 proved a real run can produce the expected LAB deliverable while the adapter honestly records `end_state: "timeout"`. Phase 4 must separate benchmark usability from raw terminal/protocol evidence.

Output: status derivation helpers, enriched metrics writing, and tests that lock the mixed-state semantics.
</objective>

<execution_context>
@/Users/houfu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/houfu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
@.planning/phases/03-implement-nanoclaw-lq-adapter/03-03-SUMMARY.md
@.planning/phases/03-implement-nanoclaw-lq-adapter/03-UAT.md
@lab_harness_runner/adapter.py
@lab_harness_runner/metrics.py
@lab_harness_runner/evaluator.py

<interfaces>
From lab_harness_runner/adapter.py:
- TaskSpec(task_id: str, instructions: str, documents_dir: Path, expected_deliverables: list[str], run_id: str)
- RunResult(run_id: str, end_state: Literal["clean","agent_error","timeout"], wall_clock_seconds: float, input_tokens: int|None = None, output_tokens: int|None = None, documents_read: int|None = None, total_vdr_files: int|None = None, documents_skipped: int|None = None, documents_read_list: list[str] = [], documents_skipped_list: list[str] = [])

From lab_harness_runner/metrics.py:
- write_metrics(run_dir: Path, result: RunResult) -> Path writes LAB-compatible metrics.json with safe defaults.

From lab_harness_runner/evaluator.py:
- score_run validates expected deliverables under lab_path/results/run_id/output before invoking LAB scoring; mirror this validation logic for diagnostics before scoring.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add benchmark status derivation tests</name>
  <files>tests/test_status.py</files>
  <behavior>
    - Test timeout raw state plus all expected deliverables present returns benchmark_status "clean", raw_end_state "timeout", terminal_status_seen false, expected_deliverables_present true, and empty missing_deliverables per D-01/D-02/D-03.
    - Test clean raw state plus deliverables present returns benchmark_status "clean", raw_end_state "clean", terminal_status_seen true, and completion_signal "STATUS:DONE".
    - Test agent_error raw state plus a missing deliverable returns benchmark_status "error", expected_deliverables_present false, and missing_deliverables with the exact filename.
    - Test timeout raw state plus a missing deliverable returns benchmark_status "timeout" and preserves raw_end_state "timeout".
    - Test absolute or traversal expected deliverable names raise ValueError rather than reading outside output_dir.
  </behavior>
  <action>
    Create tests/test_status.py using pytest and tmp_path. Build TaskSpec and RunResult instances directly. Write expected deliverable files under tmp_path / "output" for present cases. Tests must define the expected public helper as `derive_benchmark_status(task_spec: TaskSpec, output_dir: Path, result: RunResult, adapter_name: str) -> dict[str, object]`. Do not weaken D-01: valid deliverables with raw timeout must be benchmark-clean, with raw timeout preserved separately.
  </action>
  <verify>
    <automated>uv run pytest tests/test_status.py -q</automated>
  </verify>
  <done>tests/test_status.py exists and initially drives the required status contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement status diagnostics and metrics extension</name>
  <files>lab_harness_runner/status.py, lab_harness_runner/metrics.py, lab_harness_runner/__init__.py, tests/test_metrics.py</files>
  <behavior>
    - `derive_benchmark_status` returns JSON-serializable diagnostics including task_id, run_id, adapter, raw_end_state, benchmark_status, terminal_status_seen, completion_signal, expected_deliverables_present, missing_deliverables, run_dir when provided later, and output_dir as a string when provided.
    - `write_metrics` preserves all existing LAB metric keys and accepts optional `extra_fields: dict[str, object] | None = None`.
    - `write_metrics` writes diagnostic fields into metrics.json with no JSON null values; use false, empty string, empty list, zero, or omit optional keys rather than null.
  </behavior>
  <action>
    Add lab_harness_runner/status.py. Use pathlib.Path and the existing safe-relative-path helper from lab_harness_runner.task_reader to reject unsafe expected deliverable names before joining them to output_dir. Define `derive_benchmark_status(task_spec, output_dir, result, adapter_name)` exactly as tested. Derive benchmark_status as: "clean" when all expected deliverables exist; otherwise "timeout" when result.end_state is "timeout"; otherwise "error". Keep `raw_end_state = result.end_state`. Set terminal_status_seen true only for raw clean or agent_error, false for timeout. Set completion_signal to "STATUS:DONE" for raw clean, "STATUS:ERROR" for raw agent_error, and "" for timeout. Export the helper from __init__.py.

    Update write_metrics to accept `extra_fields=None`, merge it after existing LAB keys, and preserve current call sites. Preserve safe defaults for RunResult fields. Add tests in tests/test_metrics.py for diagnostic field writing and backwards compatibility with the old two-argument call.
  </action>
  <verify>
    <automated>uv run pytest tests/test_status.py tests/test_metrics.py -q</automated>
  </verify>
  <done>Status helper and metrics extension pass targeted tests and existing metrics behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite status regression check</name>
  <files>tests/test_status.py, tests/test_metrics.py</files>
  <action>
    Run the full local suite. If failures reveal legitimate compatibility issues from the metrics signature or exported helper, fix only those Phase 4 touched files. Do not modify LAB, nanoclaw-lq, or unrelated source.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <done>Full test suite is green with status/metrics diagnostics in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| output_dir filesystem -> status derivation | Files produced by an external adapter affect benchmark-facing status. |
| adapter RunResult -> metrics/reporting | Raw adapter state is untrusted evidence that must not be collapsed or hidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-R | Repudiation | status diagnostics | mitigate | Preserve `raw_end_state`, `terminal_status_seen`, and `completion_signal` alongside `benchmark_status`. |
| T-04-01-T | Tampering | expected deliverable validation | mitigate | Reject unsafe expected deliverable names before joining to output_dir; only inspect files under output_dir. |
</threat_model>

<verification>
Run `uv run pytest tests/test_status.py tests/test_metrics.py -q` after Task 2 and `uv run pytest tests/ -q` before summary.
</verification>

<success_criteria>
REQ-09 is handled because a missing terminal STATUS signal no longer automatically fails benchmark-clean deliverables, while raw timeout evidence remains visible. REQ-21 is started because metrics now represent whole system status rather than a model-only score.
</success_criteria>

<output>
Create `.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-01-SUMMARY.md` when done.
</output>
