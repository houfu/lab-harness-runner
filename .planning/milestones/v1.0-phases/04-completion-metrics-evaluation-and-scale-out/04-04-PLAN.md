---
id: 04-04
phase: 04-completion-metrics-evaluation-and-scale-out
plan: 04
title: Practical adapter guide and second-adapter compatibility check
type: execute
status: ready
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - docs/adapter-guide.md
  - tests/test_docs.py
autonomous: true
requirements: [REQ-16, REQ-21]
requirements_addressed: [REQ-16, REQ-21]
source_decisions: [D-01, D-02, D-03, D-07, D-08, D-09, D-11, D-12]
read_first:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-01-SUMMARY.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-02-SUMMARY.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-03-SUMMARY.md
  - lab_harness_runner/adapter.py
  - lab_harness_runner/status.py
  - lab_harness_runner/metrics.py
  - lab_harness_runner/aggregation.py
  - scripts/run_benchmark.py
  - docs/verified-contracts.md
acceptance_criteria:
  - docs/adapter-guide.md is practical and implementation-facing, not broad public onboarding, per D-11.
  - The guide documents TaskSpec, RunResult, Adapter.run(task_spec, output_dir) -> RunResult, deliverable validation, metrics fields, benchmark_status vs raw_end_state, completion_signal, batch summary rows, and LAB path compatibility per D-12.
  - The guide explains how a second adapter would plug in later without implementing one now, respecting deferred scope.
  - Doc tests verify required terms and ensure no deferred second-adapter implementation is introduced.
tests:
  - uv run pytest tests/test_docs.py -q
  - uv run pytest tests/ -q
threat_model:
  trust_boundaries:
    - future adapter implementer -> adapter contract
    - docs -> benchmark interpretation
  stride:
    - id: T-04-04-R
      category: Repudiation
      component: adapter guide failure semantics
      disposition: mitigate
      mitigation: Document raw_end_state and benchmark_status as separate fields with timeout-with-valid-output example.
    - id: T-04-04-I
      category: Information disclosure
      component: documentation examples
      disposition: mitigate
      mitigation: Use placeholder paths and no secrets; do not document credentials or private API keys.
must_haves:
  truths:
    - "D-11: A future adapter author gets a practical implementation guide rather than minimal notes or polished public docs."
    - "D-12: The docs include the interface contract, examples, failure semantics, deliverable validation behavior, metrics fields, benchmark-facing status vs raw/protocol status, and second-adapter guidance."
    - "D-01: The docs explain why valid deliverables can be benchmark-clean while raw timeout remains diagnostic evidence."
    - The docs preserve LAB compatibility and whole-system reporting semantics.
  artifacts:
    - path: docs/adapter-guide.md
      provides: Practical adapter implementation guide.
      contains: benchmark_status
    - path: tests/test_docs.py
      provides: Required-content checks for adapter guide.
      contains: adapter-guide.md
  key_links:
    - from: docs/adapter-guide.md
      to: lab_harness_runner/adapter.py
      via: documented interface contract
      pattern: run(task_spec, output_dir) -> RunResult
    - from: docs/adapter-guide.md
      to: scripts/run_benchmark.py
      via: documented adapter registration path
      pattern: --adapter nanoclaw
---

<objective>
Write the practical adapter guide and verify the Phase 4 contracts are documented for a later second adapter without implementing that adapter now.

Purpose: The project needs a reusable contract and honest reporting semantics that future harness adapters can follow while keeping nanoclaw-specific behavior out of the package core.

Output: `docs/adapter-guide.md` plus lightweight doc tests.
</objective>

<execution_context>
@/Users/houfu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/houfu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
@lab_harness_runner/adapter.py
@lab_harness_runner/status.py
@lab_harness_runner/metrics.py
@lab_harness_runner/aggregation.py
@scripts/run_benchmark.py
@docs/verified-contracts.md

<interfaces>
Required documented contract:
- Adapter Protocol: run(self, task_spec: TaskSpec, output_dir: Path) -> RunResult
- TaskSpec fields: task_id, instructions, documents_dir, expected_deliverables, run_id
- RunResult fields: run_id, end_state, wall_clock_seconds, token/document metrics
- Metrics diagnostics: benchmark_status, raw_end_state, terminal_status_seen, completion_signal, expected_deliverables_present, missing_deliverables
- Batch summary row fields from Plan 04-03
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adapter guide content tests</name>
  <files>tests/test_docs.py</files>
  <behavior>
    - docs/adapter-guide.md exists and contains the exact contract text `run(task_spec, output_dir) -> RunResult`.
    - Guide contains TaskSpec, RunResult, benchmark_status, raw_end_state, terminal_status_seen, completion_signal, expected_deliverables_present, missing_deliverables, metrics.json, summary.json, and `/Users/houfu/Projects/harvey-labs/results/<run-id>/`.
    - Guide contains a timeout-with-valid-deliverables example that distinguishes benchmark_status clean from raw_end_state timeout.
    - Guide mentions how to add a second adapter later but does not add or require a second adapter implementation.
  </behavior>
  <action>
    Create tests/test_docs.py with plain file-content assertions against docs/adapter-guide.md. Keep tests deterministic and local; do not use markdown parsers or external packages.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py -q</automated>
  </verify>
  <done>Doc tests define the practical guide coverage required by D-11/D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write practical adapter implementation guide</name>
  <files>docs/adapter-guide.md</files>
  <behavior>
    - The guide is concise and practical, focused on implementation steps and fields.
    - It states LAB remains unmodified and every per-run output remains under `/Users/houfu/Projects/harvey-labs/results/<run-id>/`.
    - It explains raw adapter status vs benchmark-facing status with the Phase 3 timeout/valid-deliverable case.
    - It gives a second-adapter checklist without implementing or planning a specific new adapter.
  </behavior>
  <action>
    Write docs/adapter-guide.md with sections: Contract, Implementing run(), Deliverables and validation, Metrics and status semantics, Scoring/report preservation, Batch summaries and variance, Adding another adapter later, and Checklist. Include one short Python-shaped example of a class with `run(task_spec, output_dir) -> RunResult`, but do not include long code blocks or nanoclaw-specific SQLite/mount internals as universal requirements. Use nanoclaw only as the current reference adapter. Make clear that seed is metadata unless an adapter explicitly supports deterministic seeding.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py -q</automated>
  </verify>
  <done>docs/adapter-guide.md passes required-content tests and documents the Phase 4 contract.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite and deferred-scope check</name>
  <files>docs/adapter-guide.md, tests/test_docs.py</files>
  <action>
    Run the full test suite and check for accidental second-adapter implementation files. The docs may mention second adapters as future compatibility, but this plan must not create production files for another adapter because additional adapters are deferred.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
    <automated>uv run python -c "from pathlib import Path; allowed={'adapter.py','nanoclaw_adapter.py'}; extra=[p for p in Path('lab_harness_runner').glob('*adapter.py') if p.name not in allowed]; raise SystemExit(1 if extra else 0)"</automated>
  </verify>
  <done>Guide is tested, full suite is green, and no second adapter implementation was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future adapter implementer -> protocol | Docs shape how third-party code reports outcomes. |
| docs -> benchmark interpretation | Readers may overclaim model-only or deterministic seed results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-R | Repudiation | failure semantics docs | mitigate | Include explicit benchmark_status vs raw_end_state guidance and timeout-with-valid-output example. |
| T-04-04-I | Information disclosure | examples | mitigate | Use placeholder paths and no credentials or secrets. |
| T-04-04-T | Tampering | future adapter path handling | mitigate | Document that adapters must write only to the provided output_dir and must not move LAB artifacts. |
</threat_model>

<verification>
Run `uv run pytest tests/test_docs.py -q`, then `uv run pytest tests/ -q`, then confirm no second adapter production file was created.
</verification>

<success_criteria>
REQ-16 is satisfied with a practical guide. REQ-21 is reinforced because the guide documents whole agent-system reporting and avoids model-only claims.
</success_criteria>

<output>
Create `.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-04-SUMMARY.md` when done.
</output>
