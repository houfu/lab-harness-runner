---
id: 04-02
phase: 04-completion-metrics-evaluation-and-scale-out
plan: 02
title: Primary LAB-compatible benchmark command and report preservation
type: execute
status: ready
wave: 2
depends_on: [04-01]
files_modified:
  - scripts/run_benchmark.py
  - scripts/nanoclaw_run.py
  - lab_harness_runner/evaluator.py
  - tests/test_run_benchmark.py
  - tests/test_evaluator.py
autonomous: true
requirements: [REQ-14, REQ-21]
requirements_addressed: [REQ-14, REQ-21]
source_decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]
read_first:
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-01-SUMMARY.md
  - scripts/nanoclaw_run.py
  - scripts/fake_run.py
  - lab_harness_runner/status.py
  - lab_harness_runner/evaluator.py
  - docs/verified-contracts.md
acceptance_criteria:
  - `uv run python scripts/run_benchmark.py --task <task> --adapter nanoclaw --nanoclaw-dir <path> --group-id <id> --score --report` is the primary single-run command shape per D-04/D-05/D-06.
  - The command composes read_task, build_result_dir, adapter.run, derive_benchmark_status, write_metrics, and score_run rather than replacing LAB scoring.
  - Per-run artifacts remain under `/Users/houfu/Projects/harvey-labs/results/<run-id>/`; scores_path points to scores.json and report_path points to report.html in that run folder.
  - `scripts/nanoclaw_run.py` remains available as a compatibility/dev wrapper or delegates to the new command without changing LAB layout.
tests:
  - uv run pytest tests/test_run_benchmark.py tests/test_evaluator.py -q
  - uv run pytest tests/ -q
threat_model:
  trust_boundaries:
    - CLI arguments -> LAB filesystem and nanoclaw adapter
    - benchmark command -> LAB evaluator subprocess
  stride:
    - id: T-04-02-T
      category: Tampering
      component: CLI task/run/group inputs
      disposition: mitigate
      mitigation: Validate task_id, run_id, group_id, and batch metadata with existing safe relative path helper before filesystem joins.
    - id: T-04-02-E
      category: Elevation of privilege
      component: evaluator/report subprocess
      disposition: mitigate
      mitigation: Use list-form subprocess calls with shell=False default and cwd=lab_path; do not interpolate user input into shell strings.
must_haves:
  truths:
    - A user can run, validate, score, and preserve report artifacts through one command.
    - LAB-generated scores.json and report.html are preserved in the normal run directory.
    - Benchmark output reports whole agent-system metadata, not a model-only result.
  artifacts:
    - path: scripts/run_benchmark.py
      provides: Primary benchmark command.
      contains: argparse
    - path: tests/test_run_benchmark.py
      provides: CLI orchestration tests with mocked adapter/scoring.
      contains: run_benchmark
    - path: lab_harness_runner/evaluator.py
      provides: score/report path preservation helpers.
      contains: report.html
  key_links:
    - from: scripts/run_benchmark.py
      to: lab_harness_runner.status.derive_benchmark_status
      via: status diagnostics before metrics/scoring
      pattern: derive_benchmark_status
    - from: scripts/run_benchmark.py
      to: lab_harness_runner.evaluator.score_run
      via: optional --score path
      pattern: score_run
---

<objective>
Create the one benchmark-facing command that runs the adapter, validates deliverables, writes enriched metrics, optionally invokes LAB scoring/report generation, and prints/preserves artifact paths.

Purpose: Users should not manually chain run, score, report, and metadata steps for the primary benchmark path.

Output: `scripts/run_benchmark.py`, report-path preservation, tests, and a compatibility path for `scripts/nanoclaw_run.py`.
</objective>

<execution_context>
@/Users/houfu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/houfu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-01-SUMMARY.md
@scripts/nanoclaw_run.py
@scripts/fake_run.py
@lab_harness_runner/status.py
@lab_harness_runner/evaluator.py
@docs/verified-contracts.md

<interfaces>
From Plan 04-01:
- derive_benchmark_status(task_spec, output_dir, result, adapter_name) returns benchmark_status, raw_end_state, terminal_status_seen, completion_signal, expected_deliverables_present, and missing_deliverables.
- write_metrics(run_dir, result, extra_fields=None) preserves LAB metric keys and writes diagnostic extensions.

From lab_harness_runner/evaluator.py:
- score_run(lab_path, run_id, task_id, expected_deliverables, judge_model="claude-sonnet-4-6") -> Path returns scores.json and LAB run_eval also writes report.html.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test single-run command orchestration and report paths</name>
  <files>tests/test_run_benchmark.py, tests/test_evaluator.py</files>
  <behavior>
    - A mocked single nanoclaw run calls read_task -> build_result_dir -> NanoclawAdapter.run -> derive_benchmark_status -> write_metrics.
    - With `--score --report`, score_run is called and the returned summary includes scores_path plus report_path as `<run_dir>/report.html`.
    - With no `--score`, scoring is skipped and report_path is an empty string or absent, without failing the run.
    - Unsafe `--task`, `--run-id`, or `--group-id` values fail before adapter invocation.
  </behavior>
  <action>
    Create tests/test_run_benchmark.py around importable functions from the planned script, not only subprocess execution. Expected function names: `run_single_benchmark(args) -> dict[str, object]` and `build_parser() -> argparse.ArgumentParser`. Mock NanoclawAdapter and score_run; use tmp_path as a fake LAB root with results/<run-id>/output. Extend tests/test_evaluator.py with a report path assertion only if evaluator.py adds a helper; otherwise keep report_path testing in test_run_benchmark.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_run_benchmark.py tests/test_evaluator.py -q</automated>
  </verify>
  <done>Tests define the single-command orchestration and report preservation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scripts/run_benchmark.py single-run path</name>
  <files>scripts/run_benchmark.py, scripts/nanoclaw_run.py, lab_harness_runner/evaluator.py</files>
  <behavior>
    - Parser supports `--task`, `--adapter nanoclaw`, `--run-id`, `--lab-path`, `--nanoclaw-dir`, `--group-id`, `--timeout`, `--score`, `--report`, and `--judge-model`.
    - Single-run path keeps run_dir under lab_path / "results" / run_id and does not move or copy LAB outputs elsewhere.
    - `--report` records/publishes the report.html path generated by LAB run_eval when scoring is enabled; if `--report` is passed without `--score`, return a clear parser or runtime error because report generation is coupled to LAB scoring.
    - Console output includes run_id, run_dir, output_dir, benchmark_status, raw_end_state, metrics_path, scores_path when present, and report_path when present.
  </behavior>
  <action>
    Implement scripts/run_benchmark.py using stdlib argparse and existing project helpers. Keep nanoclaw-specific construction inside an adapter factory branch for `--adapter nanoclaw`; do not introduce a second adapter. Use `_reject_unsafe_relative_path` for task/run/group inputs. Generate uuid4 run_id when omitted. After adapter.run, call derive_benchmark_status with adapter_name "nanoclaw", add run_dir/output_dir string fields to the diagnostics, then write_metrics(run_dir, result, extra_fields=diagnostics). If `--score` is passed, call score_run; set scores_path to the returned path and report_path to run_dir / "report.html" when `--report` is also passed. Preserve LAB-generated files in place. Update scripts/nanoclaw_run.py to either delegate to run_benchmark.py-compatible internals or keep a narrow legacy command with a note in the docstring; do not duplicate new status semantics in two places.
  </action>
  <verify>
    <automated>uv run pytest tests/test_run_benchmark.py tests/test_evaluator.py -q</automated>
  </verify>
  <done>The primary single-run command works under tests and preserves LAB scoring/report paths.</done>
</task>

<task type="auto">
  <name>Task 3: Command smoke and full-suite check</name>
  <files>scripts/run_benchmark.py, tests/test_run_benchmark.py</files>
  <action>
    Run a parser/help smoke and full suite. Use only mocked/unit tests for live nanoclaw and judge-dependent behavior. Do not run a paid/live nanoclaw benchmark in this autonomous plan.
  </action>
  <verify>
    <automated>uv run python scripts/run_benchmark.py --help</automated>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <done>`run_benchmark.py --help` succeeds and the full local suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> filesystem | User task/run/group flags influence paths under LAB and nanoclaw. |
| runner -> LAB evaluator | The command invokes an external Python module in the LAB checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-T | Tampering | CLI path inputs | mitigate | Use `_reject_unsafe_relative_path` before filesystem joins for task_id, run_id, and group_id. |
| T-04-02-E | Elevation of privilege | scoring/report subprocess | mitigate | Keep list-form subprocess invocation through score_run; no shell strings. |
| T-04-02-R | Repudiation | command output | mitigate | Print and write benchmark_status and raw_end_state for every run. |
</threat_model>

<verification>
Run targeted command tests, `uv run python scripts/run_benchmark.py --help`, then `uv run pytest tests/ -q`.
</verification>

<success_criteria>
REQ-14 is satisfied for the single-run path because LAB scores and reports are preserved under the normal run folder. REQ-21 is advanced because the command records adapter and status context around the score.
</success_criteria>

<output>
Create `.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-02-SUMMARY.md` when done.
</output>
