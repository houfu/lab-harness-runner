---
phase: 06-metrics-extraction-and-model-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - docs/adapter-guide.md
  - tests/test_docs.py
autonomous: false
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
user_setup: []

must_haves:
  truths:
    - "docs/adapter-guide.md contains a new 'Metrics Extraction' section with three paragraphs covering the MetricsExtractor extension point, the Anthropic vs no-op routing rule, and the cache fold note"
    - "The 'Metrics Extraction' section sits after the existing 'RunResult' field list and before 'Implementing run()'"
    - "The 'Results are whole agent-system outcomes' sentence is unchanged (D-20 explicit anchor; additive only)"
    - "tests/test_docs.py has a new regression test that asserts the doc contains the MetricsExtractor / is_claude_model / cache_creation_input_tokens / cache_read_input_tokens substrings"
    - "A live verification run command is documented in the plan (the operator runs it manually with --keep-failed) on corporate-ma/compare-matter-plan-against-engagement-letter, --model claude-opus-4-8, with --keep-failed"
    - "The plan's verification step confirms the resulting metrics.json has non-null input_tokens / output_tokens matching the transcript sums and a non-empty documents_read_list"
  artifacts:
    - path: "docs/adapter-guide.md"
      provides: "New 'Metrics Extraction' section with three paragraphs per D-20"
      contains: "## Metrics Extraction"
    - path: "tests/test_docs.py"
      provides: "test_adapter_guide_documents_metrics_extraction_section regression test"
      contains: "test_adapter_guide_documents_metrics_extraction_section"
  key_links:
    - from: "docs/adapter-guide.md::## Metrics Extraction"
      to: "lab_harness_runner/metrics_extraction.py"
      via: "Doc references the Protocol, is_claude_model, and the cache fold so a downstream consumer understands the contract"
      pattern: "MetricsExtractor"
    - from: "tests/test_docs.py::test_adapter_guide_documents_metrics_extraction_section"
      to: "docs/adapter-guide.md"
      via: "Reads the guide via guide_text() and asserts the new section's required substrings"
      pattern: "test_adapter_guide_documents_metrics_extraction_section"
    - from: "live verification run"
      to: "data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/<session_id>.jsonl"
      via: "The --keep-failed flag retains the ephemeral group + transcript for manual inspection per D-18"
      pattern: "data/v2-sessions/"
---

<objective>
Add the D-20 docs addendum to `docs/adapter-guide.md` — a new "Metrics Extraction" section with three paragraphs covering (a) the `MetricsExtractor` extension point and the one-method contract, (b) the Anthropic vs no-op routing rule (`is_claude_model` predicate with the `claude` prefix rule), and (c) the cache fold note that `input_tokens` on a Claude run is `raw + cache_creation + cache_read`. The section is additive — the existing "Results are whole agent-system outcomes" sentence is preserved unchanged (D-20 anchor). A regression test in `tests/test_docs.py` guards the new section's required substrings.

The plan also documents the D-18 / D-19 live verification run (for the operator to execute after this plan ships): one scoped run on `corporate-ma/compare-matter-plan-against-engagement-letter` with `--keep-failed --model claude-opus-4-8`, followed by manual inspection of `data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/<session_id>.jsonl` to confirm the synthetic fixture shape matches reality, and a final `metrics.json` check that `input_tokens` / `output_tokens` are non-null and `documents_read_list` is non-empty.

Purpose: This is the verification half of Phase 6. The unit tests in Plans 01 and 02 prove the extractor works against synthetic fixtures; this plan (a) writes down the contract for future adapter authors and (b) lays out the one live run the operator executes to confirm the synthetic fixtures match the real nanoclaw transcript layout. The live run is verification, not a benchmark (D-18; the 1251-task sweep is Phase 7's concern).

Output: Two files updated (`docs/adapter-guide.md` and `tests/test_docs.py`); one verification step (the operator runs the live command and inspects the output).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-metrics-extraction-and-model-routing/06-CONTEXT.md
@.planning/phases/06-metrics-extraction-and-model-routing/06-01-SUMMARY.md
@.planning/phases/06-metrics-extraction-and-model-routing/06-02-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-honest-unmeasured-metrics-contract/05-VERIFICATION.md
@docs/adapter-guide.md
@tests/test_docs.py
@scripts/run_benchmark.py
@scripts/nanoclaw_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the D-20 'Metrics Extraction' section to docs/adapter-guide.md and a regression doc test</name>
  <files>docs/adapter-guide.md, tests/test_docs.py</files>
<read_first>
  - docs/adapter-guide.md (current state: the 'RunResult' field list ends at L39, followed by 'Implementing run()' at L44. The new section uses `##` (same level as `## Contract` / `## Implementing run()`), positioned as a sibling AFTER the `## Contract` block and BEFORE `## Implementing run()`. Insert the new section BETWEEN L42 and L44 — after the 'Core package treats RunResult.end_state as adapter/protocol evidence' paragraph at L41-42, and before '## Implementing run()')
  - tests/test_docs.py (current state: existing tests cover the null-vs-zero distinction from Phase 5; add a new test function after `test_adapter_guide_documents_null_vs_zero_distinction` at L73-103)
  - .planning/phases/06-metrics-extraction-and-model-routing/06-CONTEXT.md (D-20 specifies the three paragraphs by topic: extension point, routing rule, cache fold note)
  - .planning/phases/05-honest-unmeasured-metrics-contract/05-CONTEXT.md (D-16 from Phase 5 used the same pattern: doc addendum + regression doc test)
</read_first>
<action>
Per D-20: add a new "## Metrics Extraction" section to `docs/adapter-guide.md` and a regression test in `tests/test_docs.py`.

**docs/adapter-guide.md** — INSERT a new section. The current file has the `## RunResult` field list ending at L39, then a `## Implementing run()` heading at L44. The new section goes BETWEEN them (between L42 and L44, after the "The core package treats `RunResult.end_state` as adapter/protocol evidence" paragraph and before "## Implementing run()"). The new section is exactly three paragraphs per D-20, with a one-line heading.

The new section content (three paragraphs, three topics per D-20):

Paragraph 1 — Extension point + protocol contract + return-RunResult semantics + end_state note:

"Adapter authors can plug in a model-specific metrics extractor by implementing the `MetricsExtractor` protocol from `lab_harness_runner.metrics_extraction`. The protocol is a single method, `extract(messages_out: list[dict]) -> RunResult`, that returns a fully-formed `RunResult` carrying the extractor's measurements (token counts and document coverage). The adapter base class builds its own `RunResult` from the poll loop and replaces the token / coverage fields with the extractor's output. The extractor only sees a successful transcript, so its return value's `end_state` is `\"clean\"` by definition; the adapter owns end-state mapping."

Paragraph 2 — Routing rule (the `claude` prefix predicate) + the no-op's null-everywhere + never-raise behaviour + the EXT-04 Ollama clause:

"Routing is decided at `EphemeralNanoclawAdapter` construction time using the `is_claude_model(model)` predicate: a non-empty string starting with the case-sensitive prefix `claude` selects the `AnthropicTranscriptExtractor`; anything else — `None`, `\"\"`, `ollama`, `deepseek-v4-flash:cloud`, `qwen2.5`, etc. — selects the no-op `NoOpExtractor` that returns every token / coverage field as `None` and never raises. The Ollama / unknown-model path is covered by the no-op extractor (EXT-04's \"Ollama path returns null metrics without raising\" clause)."

Paragraph 3 — Cache fold note: `input_tokens` on a Claude run is `raw + cache_creation + cache_read`; no sidecar:

"For a Claude-prefixed model, the `AnthropicTranscriptExtractor` reads `input_tokens` and `output_tokens` from the `usage` block on every assistant message in nanoclaw's transcript jsonl. `input_tokens` is the **sum of the raw `input_tokens`, `cache_creation_input_tokens`, and `cache_read_input_tokens` fields** on each line — both cache fields are folded, matching the user-facing Anthropic bill. The cache breakdown is not preserved as a sidecar field. A downstream consumer of `metrics.json` that wants the raw `input_tokens` (without cache) can subtract the cache fields themselves, but the `MetricsExtractor` does not surface them; the contract is \"the user-facing bill\" on a Claude run."

Use the section heading `## Metrics Extraction` (two `##` chars, matching the file's other top-level sections). Use a blank line BETWEEN paragraphs (markdown paragraph separator). The wording above is the suggested text per Claude's Discretion (D-20: "exact wording of the D-20 docs addendum" is a planner/executor choice). Adjust phrasing as needed but preserve the three-topic structure and the explicit `claude` prefix wording in paragraph 2. The "Results are whole agent-system outcomes" sentence at L165 is UNCHANGED (additive only — D-20 explicit anchor).

**tests/test_docs.py** — ADD a new test function `test_adapter_guide_documents_metrics_extraction_section` after the existing `test_adapter_guide_documents_null_vs_zero_distinction` (L73-103). The new test reads the guide via `guide_text()` and asserts the section's required substrings are all present. The required-substring list:

1. `"## Metrics Extraction"` (the section heading is present).
2. `"MetricsExtractor"` (the protocol name is named).
3. `"extract(messages_out: list[dict]) -> RunResult"` (the exact protocol signature is documented — matches the `lab_harness_runner.metrics_extraction` source, so a future edit that drifts the signature fails the test).
4. `"is_claude_model"` (the routing predicate is named).
5. `"claude"` (the prefix rule is mentioned — paired with `is_claude_model`).
6. `"NoOpExtractor"` (the no-op class is named — paired with `Ollama` to confirm the EXT-04 clause).
7. `"Ollama"` (the EXT-04 clause's example is documented).
8. `"cache_creation_input_tokens"` (the cache fold is documented by name — D-20 paragraph 3 regression guard).
9. `"cache_read_input_tokens"` (the cache fold is documented by name — D-20 paragraph 3 regression guard).
10. `"Results are whole agent-system outcomes"` (the existing invariant from L165 is still present — D-20 anchor).
11. `"\"clean\""` (the end_state semantics on the extractor's return is documented).

The test loops over a list of required substrings and asserts each is in the guide text, with a per-substring error message that quotes the missing term. The test's docstring explains: "Phase 6 D-20: the adapter guide must document the MetricsExtractor extension point, the Anthropic vs no-op routing rule, and the cache fold note. The 'Results are whole agent-system outcomes' invariant is preserved."

Do NOT modify any other test in `tests/test_docs.py` (the existing Phase 5 test for the null-vs-zero distinction is preserved). Do NOT modify `lab_harness_runner/`, `tests/conftest.py`, `tests/test_metrics.py`, `tests/test_nanoclaw_adapter.py`, `scripts/`, or any source file (those are Plans 01 and 02's territory).

The verification of the live run (D-18, D-19) is captured in the `<verification>` block below as a documented operator-executed step — the executor does NOT run the live command in this plan (the live run requires operator-issued CLI commands against nanoclaw, which is out of scope for an autonomous plan execution).
</action>
<verify>
  <automated>uv run --quiet python -m pytest tests/test_docs.py -v 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria>
  - `grep -n "## Metrics Extraction" docs/adapter-guide.md` returns exactly one match
  - `grep -n "MetricsExtractor" docs/adapter-guide.md` returns at least two matches (one in the section heading area, one in the paragraph body)
  - `grep -n "is_claude_model" docs/adapter-guide.md` returns at least one match
  - `grep -n "NoOpExtractor" docs/adapter-guide.md` returns at least one match
  - `grep -n "cache_creation_input_tokens" docs/adapter-guide.md` returns at least one match
  - `grep -n "cache_read_input_tokens" docs/adapter-guide.md` returns at least one match
  - `grep -n "Results are whole agent-system outcomes" docs/adapter-guide.md` returns exactly one match (the existing invariant from L165 is preserved)
  - `grep -n "test_adapter_guide_documents_metrics_extraction_section" tests/test_docs.py` returns at least one match (the new regression test)
  - `uv run --quiet python -m pytest tests/test_docs.py -q` exits 0 (all existing doc tests still pass + the new test passes)
  - The new section is INSERTED between the existing "## RunResult" field list (ending at L39) and the "## Implementing run()" heading (at L44) — confirmed by reading the file or by a `grep -n "^## " docs/adapter-guide.md` that lists the new section in the expected order
  - The new section is exactly three paragraphs separated by blank lines (confirmed by reading the file or by a runtime check that counts blank-line-separated paragraphs)
  - The new test's required-substrings list contains all eleven substrings (1-11 in the action above)
</acceptance_criteria>
<done>docs/adapter-guide.md has a new "Metrics Extraction" section with three paragraphs per D-20 (extension point, routing rule, cache fold note). tests/test_docs.py has a new regression test asserting the section's required substrings. The "Results are whole agent-system outcomes" invariant is preserved unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-executed live verification run with --keep-failed (D-18, D-19)</name>
  <files>~/Projects/nanoclaw-lq/data/v2-sessions/&lt;group_id&gt;/.claude-shared/projects/-workspace-agent/&lt;session_id&gt;.jsonl, ~/Projects/harvey-labs/results/&lt;run_id&gt;/metrics.json</files>
  <action>Operator executes the D-18 live verification command on the v1.0 proof task `corporate-ma/compare-matter-plan-against-engagement-letter` with `--model claude-opus-4-8 --keep-failed`, then inspects the resulting nanoclaw transcript jsonl and the per-run metrics.json to confirm the synthetic fixture shape matches reality. The exact command sequence, transcript inspection steps, and metrics.json assertions are documented in the `&lt;how-to-verify&gt;` block below. Per D-19, if the path layout deviates from D-04's expectation, the operator describes the deviation in the resume signal so a follow-up resolver amendment can be planned (small code change, no surface change).</action>
  <verify>The operator confirms via the resume signal that (a) the transcript jsonl at `~/Projects/nanoclaw-lq/data/v2-sessions/&lt;group_id&gt;/.claude-shared/projects/-workspace-agent/&lt;session_id&gt;.jsonl` carries `usage.input_tokens` / `usage.output_tokens` / `usage.cache_creation_input_tokens` / `usage.cache_read_input_tokens` on assistant messages AND `Read` tool_use blocks with `input.file_path`; (b) the per-run `metrics.json` has non-null `input_tokens` and `output_tokens` matching the transcript sums and a non-empty `documents_read_list`; (c) the full unit test suite remains green after the live run.</verify>
  <done>The live run completed; the operator typed "approved" (or described a D-19 deviation); the schema discovery confirms the unit tests' synthetic fixture shape matches reality OR a follow-up resolver amendment is planned and documented in the plan SUMMARY.</done>
<read_first>
  - .planning/phases/06-metrics-extraction-and-model-routing/06-CONTEXT.md (D-18 specifies the scoped task; D-19 specifies the path-layout-fallback amendment if the layout differs)
  - scripts/run_benchmark.py (the --model and --keep-failed flags are wired through to EphemeralNanoclawAdapter; the operator runs this script)
  - scripts/nanoclaw_run.py (alternative single-task runner that exercises the same adapter; either script is acceptable)
  - 06-01-SUMMARY.md and 06-02-SUMMARY.md (the wiring + unit tests are already in place by the time this task runs; the live run is the schema-discovery confirmation)
</read_first>
<what-built>
The MetricsExtractor module (Plan 01), the EphemeralNanoclawAdapter wiring + integration tests (Plan 02), and the docs addendum + regression doc test (Plan 03 Task 1) are all in place. The unit tests prove the extractor works against synthetic fixtures. This task is the D-18 / D-19 schema-discovery verification: one live ephemeral-group run on the v1.0 proof task with --keep-failed, followed by manual inspection of the resulting nanoclaw transcript jsonl and the per-run metrics.json.

The live run goal is **schema discovery**, not a benchmark. The unit tests' synthetic fixture shape (assistant messages with `usage.input_tokens` / `usage.output_tokens` / `usage.cache_creation_input_tokens` / `usage.cache_read_input_tokens`, and `Read` tool_use blocks with `input.file_path`) is verified against reality by inspecting the real nanoclaw transcript. The 1251-task sweep is Phase 7's concern; this run is verification, not throughput.
</what-built>
<how-to-verify>
The operator executes the following sequence on a machine that has the `nanoclaw-lq` repo checked out and the docker daemon running (the v1.0 proof environment):

1. Run the scoped task with --keep-failed so the ephemeral group + transcript survive for inspection:
   ```bash
   uv run python scripts/run_benchmark.py \
     --task corporate-ma/compare-matter-plan-against-engagement-letter \
     --adapter nanoclaw \
     --nanoclaw-dir ~/Projects/nanoclaw-lq \
     --model claude-opus-4-8 \
     --keep-failed \
     --output-dir ~/Projects/harvey-labs/results
   ```
   The `--keep-failed` flag retains the ephemeral group regardless of success or failure (it is the operator's debug toggle; the run is expected to succeed for the v1.0 proof task).

2. After the run completes, identify the surviving group_id. The script prints `[ephemeral] keeping failed group for debugging: <group_id>` to stderr ONLY if the run failed; for a successful run, the group is destroyed by default — so for a SUCCESSFUL run the operator must pass `--keep-failed` (which the command above already does; verify the per-run summary / final `summary.json` to confirm the group was kept). If the run succeeded and the group was destroyed, the schema-discovery step still works against the LAST run's transcript if the operator notes the group_id from the logs (the run_benchmark.py output includes a "kept group" line; alternatively inspect the most recent `data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/*.jsonl`).

3. Inspect the transcript jsonl:
   ```bash
   ls ~/Projects/nanoclaw-lq/data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/
   cat ~/Projects/nanoclaw-lq/data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/<session_id>.jsonl | head -20
   ```
   Confirm:
   - Assistant messages carry `message.usage.input_tokens`, `message.usage.output_tokens`, `message.usage.cache_creation_input_tokens`, `message.usage.cache_read_input_tokens` (the four fields the extractor reads).
   - Assistant messages carry `message.content[]` blocks of type `tool_use` with `name: "Read"` and `input.file_path` set to a container-internal path.
   - The `sessionId` field appears at a known location (top-level on the first line, per the test helper's assumption, OR on every line, OR on a per-message basis — the resolver's "first line's sessionId matches the shim's sessionId" rule needs verification).

4. Inspect the per-run `metrics.json`:
   ```bash
   cat ~/Projects/harvey-labs/results/<run_id>/metrics.json
   ```
   Confirm:
   - `input_tokens` is a non-null integer equal to the sum of `raw + cache_creation + cache_read` across the assistant messages (D-05 cache fold).
   - `output_tokens` is a non-null integer equal to the sum of `output_tokens` across the assistant messages.
   - `documents_read_list` is a non-empty list of container-internal paths (one entry per `Read` tool_use block, deduplicated with order preserved).
   - `documents_read` equals `len(documents_read_list)`.
   - `end_state` is `"clean"` (the v1.0 proof task is a well-formed run that produced a `STATUS: DONE`).
   - `metrics_provided` is `true` in the batch summary (after `write_batch_summary`).

5. If the transcript path layout DIFFERS from D-04's expected layout (e.g. sessionId is not on the first line, or the file naming pattern is different), the resolver in `lab_harness_runner/metrics_extraction.py` may need a small amendment (D-19). The amendment is a small change — e.g. glob for the newest jsonl in the group dir, or change the sessionId match to scan every line — and does NOT change the extractor surface. Flag the deviation in the plan SUMMARY so a follow-up patch can be planned.

6. After the live run confirms the schema, run the full unit test suite one more time:
   ```bash
   uv run --quiet python -m pytest -q
   ```
   All tests should remain green.

The verification is complete when the operator confirms (via typing "approved" in the resume signal below) that the live transcript matches the synthetic fixture shape and `metrics.json` carries the expected non-null values. If the layout differs, the operator describes the deviation so a follow-up amendment can be planned.
</how-to-verify>
<resume-signal>Type "approved" once the live run's metrics.json shows non-null input_tokens / output_tokens matching the transcript sums and a non-empty documents_read_list. If the transcript path layout differs from D-04 (e.g. sessionId is on every line, not just the first), describe the deviation so a follow-up patch can amend the resolver per D-19.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/adapter-guide.md` → future adapter authors | The new section is the human-readable contract; the regression test in `tests/test_docs.py` is the machine-readable guard |
| Live run operator → nanoclaw transcript on disk | The operator inspects `data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/<session_id>.jsonl` manually after `--keep-failed` retains the group |
| `metrics.json` on disk → downstream consumer (LAB's `evaluation.compare`, future consumers) | The `input_tokens` / `output_tokens` / `documents_read_list` fields carry the measured run's evidence; the cache fold note in the doc tells the consumer what `input_tokens` means on a Claude run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure (docs) | The "Metrics Extraction" section names `NoOpExtractor`, `is_claude_model`, and the cache fold; a reader could infer internal nanoclaw layout details from the doc | accept | The doc is for adapter authors and operators; the names are intentional. The "runner stays thin" lock (PROJECT.md) does not restrict doc detail. |
| T-06-10 | Tampering via doc drift | A future edit to `docs/adapter-guide.md` could quietly drop the cache fold note, leaving downstream consumers surprised that `input_tokens` is the cached bill | mitigate | The regression test `test_adapter_guide_documents_metrics_extraction_section` in `tests/test_docs.py` asserts the cache fold substrings (`cache_creation_input_tokens`, `cache_read_input_tokens`) are present. A future edit that drops the wording fails the test. |
| T-06-11 | Live-run schema deviation (D-19) | The real nanoclaw transcript path layout could differ from D-04's expected `nanoclaw_dir/data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/<session_id>.jsonl` (e.g. sessionId on every line, not the first) | mitigate | D-19 explicitly anchored "small amendment, no surface change": the resolver can be amended to glob for the newest jsonl OR scan every line for the sessionId match. The unit tests against synthetic fixtures (Plan 01) and the integration tests against the conftest fixture (Plan 02) remain valid; only the resolver's matching rule changes. The operator reports the deviation in the resume signal. |
| T-06-12 | Live-run group retention | The `--keep-failed` flag retains the ephemeral group and its transcript; an unprivileged operator might be exposed to the container-internal paths in the transcript | accept | This is the operator's debug toggle, explicitly named in D-18 as a verification affordance. The container-internal paths in `documents_read_list` (e.g. `/tmp/engagement.txt`) are already accepted as the contract per D-08 ("what the agent read", not "what is in `documents_dir`"). The retention is per-run, not per-sweep. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. The live run uses the existing `uv run python scripts/run_benchmark.py` invocation. |

</threat_model>

<verification>
- `uv run --quiet python -m pytest tests/test_docs.py -q` is green (the new regression test passes alongside the existing four doc tests).
- `docs/adapter-guide.md` contains a new "## Metrics Extraction" section with three paragraphs covering (a) the `MetricsExtractor` extension point, (b) the Anthropic vs no-op routing rule, (c) the cache fold note.
- The "Results are whole agent-system outcomes" invariant at L165 is preserved (additive only — D-20 anchor; verified by `grep -c` returns exactly 1).
- The new doc test asserts all eleven required substrings (the section heading, the protocol name, the protocol signature, the routing predicate, the prefix, the no-op class, the Ollama example, both cache field names, the invariant sentence, the end_state semantics).
- The full test suite (`uv run --quiet python -m pytest -q`) is green.

Operator-executed live verification (D-18, D-19) — OUT OF SCOPE for autonomous execution; the operator runs:

```bash
uv run python scripts/run_benchmark.py \
  --task corporate-ma/compare-matter-plan-against-engagement-letter \
  --adapter nanoclaw \
  --nanoclaw-dir ~/Projects/nanoclaw-lq \
  --model claude-opus-4-8 \
  --keep-failed \
  --output-dir ~/Projects/harvey-labs/results
```

The operator confirms:
1. The transcript jsonl at `~/Projects/nanoclaw-lq/data/v2-sessions/<group_id>/.claude-shared/projects/-workspace-agent/<session_id>.jsonl` carries the `usage.input_tokens` / `usage.output_tokens` / `usage.cache_creation_input_tokens` / `usage.cache_read_input_tokens` fields on assistant messages, AND `Read` tool_use blocks with `input.file_path` on assistant messages.
2. The per-run `metrics.json` has `input_tokens` / `output_tokens` non-null and equal to the sum of the per-message `usage` blocks (with the cache fold applied), and `documents_read_list` non-empty with one entry per unique `Read` block.
3. The "sessionId" location on the real transcript matches the resolver's assumption (top-level on the first line, OR on every line — the resolver's "first match wins" rule covers both readings; flag the deviation if neither reading holds).
4. The full unit test suite remains green after the live run.

If the live run reveals a transcript path layout that differs from D-04's expectation, the plan is amended per D-19 (small code change, no surface change). The 1251-task sweep remains Phase 7's concern.
</verification>

<success_criteria>
- `docs/adapter-guide.md` has a new "## Metrics Extraction" section with three paragraphs per D-20.
- `tests/test_docs.py` has a new `test_adapter_guide_documents_metrics_extraction_section` regression test that asserts all eleven required substrings.
- The "Results are whole agent-system outcomes" invariant at L165 is preserved.
- The full test suite is green.
- The operator has confirmed (or flagged) the live run's schema discovery on `corporate-ma/compare-matter-plan-against-engagement-letter` with `--model claude-opus-4-8 --keep-failed`.
- If the live run revealed a layout deviation, the deviation is documented in the plan SUMMARY and a follow-up patch is planned per D-19.
</success_criteria>

<output>
Create `.planning/phases/06-metrics-extraction-and-model-routing/06-03-SUMMARY.md` when done. The SUMMARY should record whether the live run passed (operator typed "approved") or whether a D-19 amendment is needed (operator described the deviation).
</output>
