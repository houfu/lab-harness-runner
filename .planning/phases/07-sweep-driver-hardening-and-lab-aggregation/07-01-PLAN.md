---
phase: 07-sweep-driver-hardening-and-lab-aggregation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sweep.sh
autonomous: true
requirements: [SWP-01, SWP-02]
must_haves:
  truths:
    - "scripts/sweep.sh documents the TIMEOUT 600s default with a wall-clock rationale"
    - "An operator reading the script knows how to override TIMEOUT"
    - "sweep.sh inventory prints a human-readable header followed by bare path lines"
    - "A CI consumer can pipe inventory path lines directly into xargs with no FAILED/MISSING: prefix"
  artifacts:
    - path: "scripts/sweep.sh"
      provides: "Documented TIMEOUT default and dual-output inventory function"
      contains: "p99"
  key_links:
    - from: "scripts/sweep.sh inventory"
      to: "xargs CI consumer"
      via: "bare path lines on stdout after a --- separator"
      pattern: "echo \"\\$RESULTS"
---

<objective>
Document the `TIMEOUT` 600s default in `scripts/sweep.sh` with a rationale grounded in
observed `results/` wall-clock data (SWP-01), and rewrite the `inventory()` function so
its output is both human-readable (header counts) and machine-readable (bare path lines
consumable by `xargs`, no `FAILED/MISSING:` prefix) (SWP-02).

Purpose: Make the sweep driver's two operator-facing surfaces (the TIMEOUT knob and the
inventory report) self-documenting and CI-consumable. These are the two lowest-risk,
documentation-and-reformatting changes; they land first so later plans build on a clean base.

Output: Modified `scripts/sweep.sh` with an updated TIMEOUT comment block and a rewritten
`inventory()` function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-CONTEXT.md
@.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the TIMEOUT 600s default rationale (SWP-01, D-07)</name>
  <files>scripts/sweep.sh</files>
  <read_first>
    - scripts/sweep.sh (current TIMEOUT comment block at lines 36-40; the value line `TIMEOUT="${TIMEOUT:-600}"`)
    - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-PATTERNS.md (Sub-pattern A — exact comment text)
    - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-CONTEXT.md (D-07 figures)
  </read_first>
  <action>
    Replace the existing comment block above `TIMEOUT="${TIMEOUT:-600}"` (currently lines 36-39) with a
    rationale comment. Per D-07 the canonical (locked) figures are: p99 of clean runs = 586.2s (n=137),
    max = 596.1s; 600s catches p99+ while bounding non-deliverable stalls; the deliverable-gated poll
    short-circuits early for tasks that finish, so the ceiling only applies to tasks that produce no
    deliverable (timeouts, hard crashes). Include a one-line override note with the literal command
    `TIMEOUT=300 scripts/sweep.sh`. Do NOT change the value line itself — it stays `TIMEOUT="${TIMEOUT:-600}"`.
    Keep the comment as `#`-prefixed lines immediately preceding the value line. Wording is Claude's
    discretion per D-07 but the figures p99=586.2, n=137, max=596.1, and the token `600s` MUST appear verbatim.
  </action>
  <verify>
    <automated>grep -q 'p99' scripts/sweep.sh && grep -q '586.2' scripts/sweep.sh && grep -q 'TIMEOUT=300' scripts/sweep.sh && bash -n scripts/sweep.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '586.2' scripts/sweep.sh` returns >= 1
    - `grep -q 'p99' scripts/sweep.sh` exits 0
    - `grep -q 'TIMEOUT=300 scripts/sweep.sh' scripts/sweep.sh` exits 0
    - `grep -q 'TIMEOUT="\${TIMEOUT:-600}"' scripts/sweep.sh` exits 0 (value line unchanged)
    - `bash -n scripts/sweep.sh` exits 0 (no syntax error introduced)
  </acceptance_criteria>
  <done>The TIMEOUT comment block records the p99/max rationale and override command; the value line is unchanged; the script parses.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite inventory() for dual-output (SWP-02, D-08)</name>
  <files>scripts/sweep.sh</files>
  <read_first>
    - scripts/sweep.sh (current `inventory()` function, lines 85-93; the `is_clean` helper at lines 59-62; the `$RESULTS` and `$TASK_LIST` variables; `build_task_list` at lines 53-57)
    - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-PATTERNS.md (Sub-pattern C — exact replacement function; Bash 3.x array note)
    - .planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-CONTEXT.md (D-08)
  </read_first>
  <action>
    Replace the entire `inventory()` function body. New behavior: lazily init via
    `[ -f "$TASK_LIST" ] || build_task_list`; iterate the task list with `while IFS= read -r task; do`
    guarded by `[ -n "$task" ] || continue`; maintain `local total`, `local clean_count`, `local incomplete`
    counters and a `local paths=()` indexed array. For each task, reuse `is_clean "${task//\//__}"`: if clean,
    increment clean_count; else increment incomplete and append `"$RESULTS/${task//\//__}"` to `paths`.
    After the loop, emit the human-readable header on stdout as three lines: `total: $total`, `clean: $clean_count`,
    `incomplete: $incomplete`. Then emit a literal `---` separator line (this is the chosen separator per the
    D-08 discretion grant — a CI consumer skips the header with `tail -n +5` or `grep -v '^[a-z]\|^---'`).
    Then emit one bare path per line from `paths` with NO prefix. Do NOT use associative arrays (bash 3.2 on
    macOS lacks them). Do NOT move `inventory()` into the xargs invocation — indexed arrays are only safe when
    `inventory()` is called from `main()`. Add a one-line comment above the header echoes noting the CI skip
    technique (`# CI: skip header with tail -n +5 or grep -v '^[a-z]\|^---'`).
  </action>
  <verify>
    <automated>bash -n scripts/sweep.sh && bash scripts/sweep.sh inventory 2>/dev/null | grep -q '^---' && ! (bash scripts/sweep.sh inventory 2>/dev/null | grep -q 'FAILED/MISSING:')</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/sweep.sh` exits 0
    - `bash scripts/sweep.sh inventory` prints lines `total:`, `clean:`, `incomplete:` and a `---` separator
    - `bash scripts/sweep.sh inventory | grep -c 'FAILED/MISSING:'` returns 0 (prefix removed)
    - `bash scripts/sweep.sh inventory | tail -n +5` produces only bare filesystem path lines (each begins with the `$RESULTS` directory path), no prefixes
    - Source still contains `is_clean "${task//\//__}"` (helper reused, not reimplemented)
  </acceptance_criteria>
  <done>inventory() prints a 3-line counts header, a `---` separator, then bare result-directory paths; no FAILED/MISSING: prefix remains; script parses and runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → sweep.sh | Operator-supplied `TIMEOUT` env var crosses into the script |
| sweep.sh stdout → CI consumer | inventory path lines are consumed downstream by `xargs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `TIMEOUT` env var value | accept | Value flows only to `--timeout` flag of run_benchmark.py (integer-consuming); no shell eval; documentation-only change in this plan |
| T-07-02 | Information Disclosure | inventory stdout path lines | accept | Paths are local result directories under `$RESULTS`; no secrets; intended output for CI |
| T-07-03 | Tampering | task paths in `paths=()` array | mitigate | Task paths come from `find` on the LAB tasks dir (not runtime user input); all expansions quoted (`"$RESULTS/${task//\//__}"`) |
</threat_model>

<verification>
- `bash -n scripts/sweep.sh` exits 0
- `grep 'p99\|586.2\|TIMEOUT=300' scripts/sweep.sh` shows all three rationale tokens
- `bash scripts/sweep.sh inventory` shows header + `---` + bare paths with no `FAILED/MISSING:` prefix
</verification>

<success_criteria>
- SWP-01: TIMEOUT default documented in-script with p99=586.2s/max=596.1s rationale and override note
- SWP-02: inventory output is dual (human header + machine bare paths), CI-consumable via tail/grep
- No regression: `bash -n scripts/sweep.sh` exits 0; `is_clean` and `build_task_list` reused unchanged
</success_criteria>

<output>
Create `.planning/phases/07-sweep-driver-hardening-and-lab-aggregation/07-01-SUMMARY.md` when done
</output>
