---
id: 04-03
phase: 04-completion-metrics-evaluation-and-scale-out
plan: 03
title: Multi-task multi-seed aggregation and variance reporting
type: execute
status: ready
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - lab_harness_runner/aggregation.py
  - lab_harness_runner/__init__.py
  - scripts/run_benchmark.py
  - tests/test_aggregation.py
  - tests/test_run_benchmark.py
autonomous: true
requirements: [REQ-15, REQ-21, REQ-22]
requirements_addressed: [REQ-15, REQ-21, REQ-22]
source_decisions: [D-07, D-08, D-09, D-10]
read_first:
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-01-SUMMARY.md
  - .planning/phases/04-completion-metrics-evaluation-and-scale-out/04-02-SUMMARY.md
  - scripts/run_benchmark.py
  - lab_harness_runner/status.py
  - docs/verified-contracts.md
acceptance_criteria:
  - Command supports repeated runs from task x seed inputs while every per-run folder remains under `/Users/houfu/Projects/harvey-labs/results/<run-id>/`.
  - Aggregate metadata references run_id, run_dir, output_dir, metrics_path, scores_path, report_path, task, seed, adapter, benchmark_status, raw_end_state, and deliverable validation fields.
  - Aggregate output is metadata-only at `results/batches/<batch-id>/summary.json` or a safer flat aggregate path; it never writes aggregate scores.json or fake LAB run config.
  - Variance fields include count, mean, min, max, and sample stdev/0.0 for singleton across score and available operational metrics before any performance claim.
tests:
  - uv run pytest tests/test_aggregation.py tests/test_run_benchmark.py -q
  - uv run pytest tests/ -q
threat_model:
  trust_boundaries:
    - batch CLI inputs -> repeated filesystem writes
    - aggregate metadata -> LAB results tree
  stride:
    - id: T-04-03-T
      category: Tampering
      component: batch summary placement
      disposition: mitigate
      mitigation: Write only summary.json metadata under results/batches/<batch-id>/ or a flat aggregate JSON; never write aggregate scores.json.
    - id: T-04-03-R
      category: Repudiation
      component: variance/performance claims
      disposition: mitigate
      mitigation: Include per-run rows and variance fields so claims can be traced to raw run IDs.
must_haves:
  truths:
    - A user can run multiple tasks and seeds from one command invocation.
    - Aggregate summary references normal LAB result paths and does not move outputs.
    - Variance is reported before multi-task performance claims.
  artifacts:
    - path: lab_harness_runner/aggregation.py
      provides: Batch summary and variance helpers.
      contains: summarize_variance
    - path: scripts/run_benchmark.py
      provides: Batch CLI flags and task/seed loop.
      contains: --tasks
    - path: tests/test_aggregation.py
      provides: Unit coverage for rows, paths, and variance.
      contains: test_variance
  key_links:
    - from: scripts/run_benchmark.py
      to: lab_harness_runner.aggregation
      via: batch summary after per-run execution
      pattern: write_batch_summary
    - from: lab_harness_runner/aggregation.py
      to: /Users/houfu/Projects/harvey-labs/results/batches/<batch-id>/summary.json
      via: metadata-only aggregate
      pattern: summary.json
---

<objective>
Add batch execution and metadata-only aggregation for task x seed sweeps, with variance fields before performance claims.

Purpose: Phase 4 must support repeated benchmark runs without breaking LAB's expected result layout or polluting LAB comparison scans.

Output: aggregation helpers, batch CLI flags, tests, and summary JSON under the LAB results tree that references normal run directories.
</objective>

<execution_context>
@/Users/houfu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/houfu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-CONTEXT.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-RESEARCH.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-01-SUMMARY.md
@.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-02-SUMMARY.md
@scripts/run_benchmark.py
@docs/verified-contracts.md

<interfaces>
From Plan 04-02:
- run_single_benchmark(args) -> dict[str, object] returns run_id, run_dir, output_dir, metrics_path, benchmark_status, raw_end_state, expected_deliverables_present, missing_deliverables, scores_path, and report_path when applicable.
- build_parser() returns the primary command parser.

Aggregate rows must include: batch_id, task_id, seed, adapter, run_id, run_dir, output_dir, metrics_path, scores_path, report_path, benchmark_status, raw_end_state, terminal_status_seen, expected_deliverables_present, missing_deliverables, score, all_pass, wall_clock_seconds, input_tokens, output_tokens, documents_read, total_vdr_files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aggregation and variance tests</name>
  <files>tests/test_aggregation.py, tests/test_run_benchmark.py</files>
  <behavior>
    - summarize_variance([]) returns count 0 and null-free empty values or omitted numeric summaries.
    - summarize_variance([1.0]) returns count 1, mean/min/max 1.0, stdev 0.0.
    - summarize_variance([1.0, 3.0]) returns count 2, mean 2.0, min 1.0, max 3.0, and positive sample stdev.
    - write_batch_summary writes metadata-only summary.json with rows and variance fields, never scores.json.
    - Batch CLI expansion calls run_single_benchmark once per task x seed and records seed as metadata, not as a deterministic adapter guarantee.
  </behavior>
  <action>
    Create tests/test_aggregation.py with pure unit tests for `summarize_variance(values)`, `build_summary(rows)`, and `write_batch_summary(lab_path, batch_id, rows)`. Extend tests/test_run_benchmark.py for parser flags `--tasks`, repeated `--task`, `--seeds`, and `--batch-id`. Tests must assert aggregate paths live under lab_path / "results" and contain no file named scores.json.
  </action>
  <verify>
    <automated>uv run pytest tests/test_aggregation.py tests/test_run_benchmark.py -q</automated>
  </verify>
  <done>Tests define batch metadata, summary placement, and variance requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement aggregation helpers and batch CLI loop</name>
  <files>lab_harness_runner/aggregation.py, lab_harness_runner/__init__.py, scripts/run_benchmark.py</files>
  <behavior>
    - `summarize_variance` uses stdlib statistics and returns count, mean, min, max, stdev with stdev 0.0 for singleton.
    - `write_batch_summary` writes JSON to `lab_path/results/batches/<batch-id>/summary.json` unless implementation evidence shows a flat path is safer; either way, the file must be metadata-only and not named scores.json.
    - Batch command accepts `--task` multiple times or `--tasks <file>` plus `--seeds 1,2,3`; each task/seed produces a distinct run_id and a normal LAB run folder.
    - Seed is recorded in row metadata only unless a future adapter explicitly supports deterministic seeding; do not claim deterministic reproducibility.
  </behavior>
  <action>
    Add lab_harness_runner/aggregation.py with JSON-serializable helpers: `summarize_variance`, `build_summary`, and `write_batch_summary`. Include variance over score, wall_clock_seconds, input_tokens, output_tokens, documents_read, and total_vdr_files when values are present. Export public helpers from __init__.py.

    Extend scripts/run_benchmark.py while preserving the single-run behavior from Plan 04-02. Add batch parsing for repeated `--task`, optional `--tasks` text file with one task_id per non-empty non-comment line, `--seeds` comma-separated values, and `--batch-id`. For each task/seed, call the existing single-run function with a unique run_id derived from uuid4 when the user did not specify one. If a fixed `--run-id` is passed with more than one run, raise a clear error to avoid path collisions. After all runs, write the batch summary and print its path. Keep every run under LAB `results/<run-id>/`; only the aggregate metadata file may live under `results/batches/<batch-id>/summary.json`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_aggregation.py tests/test_run_benchmark.py -q</automated>
  </verify>
  <done>Batch task/seed loops and variance summaries are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite aggregation regression check</name>
  <files>lab_harness_runner/aggregation.py, scripts/run_benchmark.py, tests/test_aggregation.py, tests/test_run_benchmark.py</files>
  <action>
    Run the full suite and inspect aggregate test fixtures for accidental LAB pollution. The implementation must not create an aggregate scores.json anywhere under the fake LAB results tree.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
    <automated>uv run python -c "from pathlib import Path; raise SystemExit(1 if any(Path('/Users/houfu/Projects/harvey-labs/results').glob('batches/*/scores.json')) else 0)"</automated>
  </verify>
  <done>Full suite is green and no aggregate scores.json is produced under LAB results/batches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| batch CLI -> repeated runs | User-provided task and seed lists expand into multiple result directories. |
| aggregate metadata -> LAB result tree | Summary files live near LAB artifacts and must not be mistaken for scored runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-T | Tampering | aggregate summary placement | mitigate | Never write aggregate `scores.json` or fake LAB run config; use metadata-only `summary.json`. |
| T-04-03-R | Repudiation | variance reporting | mitigate | Store per-run rows and variance inputs so claims trace back to run_id and scores_path. |
| T-04-03-D | Denial of Service | batch expansion | accept | Batch size is user-selected local CLI work; no service endpoint is exposed. |
</threat_model>

<verification>
Run `uv run pytest tests/test_aggregation.py tests/test_run_benchmark.py -q`, then `uv run pytest tests/ -q`.
</verification>

<success_criteria>
REQ-15 is satisfied by task x seed execution. REQ-21 is satisfied by whole-system rows. REQ-22 is satisfied by variance summaries before any multi-task performance claim.
</success_criteria>

<output>
Create `.planning/phases/04-completion-metrics-evaluation-and-scale-out/04-03-SUMMARY.md` when done.
</output>
