---
phase: "02-build-harness-neutral-package-core"
plan: "05"
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - scripts/fake_run.py
autonomous: true
requirements:
  - Keep package-owned code harness-agnostic
  - Define an adapter contract equivalent to run(task_spec, output_dir) -> RunResult
  - Create results/<run-id>/output/ for each run
  - Write metrics.json using available RunResult metrics and safe defaults for missing values
must_haves:
  truths:
    - "fake_run.py imports all public symbols from lab_harness_runner (not internal modules)"
    - "FakeAdapter class satisfies Adapter Protocol structurally without inheriting from Adapter"
    - "fake_run.py --task <area/slug> exits 0 and creates the run directory with metrics.json"
    - "fake_run.py validates task_id and run_id with reject_unsafe_relative_path before use"
    - "fake_run.py writes placeholder deliverables (.docx as minimal valid ZIP, others as text)"
    - "metrics.json exists in the run directory after fake_run.py completes"
  artifacts:
    - path: "scripts/fake_run.py"
      provides: "Phase 2 exit criterion: end-to-end package wiring proof"
      exports: []
  key_links:
    - from: "scripts/fake_run.py"
      to: "lab_harness_runner"
      via: "from lab_harness_runner import Adapter, RunResult, TaskSpec, build_result_dir, read_task, score_run, write_metrics"
      pattern: "from lab_harness_runner import"
    - from: "scripts/fake_run.py"
      to: "lab_path/results/<run-id>/metrics.json"
      via: "write_metrics(run_dir, result)"
      pattern: "write_metrics"
---

<objective>
Create scripts/fake_run.py — the Phase 2 exit criterion. This standalone script imports
from lab_harness_runner and exercises the full pipeline: read task → create run dir →
run fake adapter → write metrics. Optionally invokes the evaluator with --score.

Purpose: D-12 requires fake_run.py to prove the package public API is correctly wired
end-to-end. FakeAdapter demonstrates the Adapter Protocol works structurally.

Output: scripts/fake_run.py that exits 0, creates a valid run directory, and writes
metrics.json when run against a real LAB task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-build-harness-neutral-package-core/02-04-SUMMARY.md

<interfaces>
<!-- Public API imported by fake_run.py -->

from lab_harness_runner import (
    Adapter,       # Protocol — for type annotation only; FakeAdapter does NOT inherit it
    RunResult,
    TaskSpec,
    build_result_dir,
    read_task,
    score_run,
    write_metrics,
)

score_run signature:
  score_run(lab_path: Path, run_id: str, task_id: str, expected_deliverables: list[str], judge_model: str) -> Path

Path resolution helper (defined locally in fake_run.py — not exported from package):
  _lab_path(override: Path | None = None) -> Path
    - Returns override if provided
    - Returns Path(os.environ["HARVEY_LAB_PATH"]) if set
    - Returns Path.home() / "Projects" / "harvey-labs" as fallback

Path safety guard (defined locally in fake_run.py — not exported from package):
  def reject_unsafe_relative_path(value: str, name: str) -> Path
    (copy from scripts/lab_probe.py lines 13-19)

Minimal DOCX writer (defined locally in fake_run.py):
  write_minimal_docx(path: Path, lines: list[str]) -> None
    (copy from scripts/lab_probe.py lines 39-70)
    Needed for --score mode with DOCX deliverables; also used in no-score mode for completeness

CLI interface:
  --task TASK           area/slug task path (required)
  --run-id RUN_ID       explicit run ID (default: str(uuid.uuid4()))
  --lab-path LAB_PATH   explicit LAB root (default: env var / home fallback)
  --score               invoke LAB evaluator after writing deliverables (default: off)
  --judge-model MODEL   judge model name (default: claude-sonnet-4-6)

Exit criterion (D-12):
  uv run python scripts/fake_run.py --task antitrust-competition/analyze-antitrust-hsr-strategy
  Must exit 0. Must create run directory. Must write metrics.json.
  Scoring is opt-in (requires --score flag and valid API credentials).

FakeAdapter contract:
  class FakeAdapter:
      def run(self, task_spec: TaskSpec, output_dir: Path) -> RunResult:
          - For each filename in task_spec.expected_deliverables:
            - If suffix is .docx: write minimal valid DOCX via write_minimal_docx
            - Otherwise: write plain text placeholder
          - Record wall_clock_seconds with time.monotonic()
          - Return RunResult(run_id=task_spec.run_id, end_state="clean", wall_clock_seconds=...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement scripts/fake_run.py</name>
  <files>scripts/fake_run.py</files>
  <read_first>
    - scripts/lab_probe.py — full file: CLI pattern, reject_unsafe_relative_path (lines 13-19), write_minimal_docx (lines 39-70), write_dummy_deliverable (lines 73-85), main structure (lines 109-151)
    - .planning/phases/02-build-harness-neutral-package-core/02-PATTERNS.md — fake_run.py section, shared patterns section
    - .planning/phases/02-build-harness-neutral-package-core/02-RESEARCH.md — Pattern 9 (fake_run structure), Run-ID Generation Recommendation, __init__.py Exports Recommendation
    - .planning/phases/02-build-harness-neutral-package-core/02-CONTEXT.md — D-12 (exit criterion), specific ideas section
    - lab_harness_runner/__init__.py — verify exported names before writing imports
  </read_first>
  <action>
    Create scripts/fake_run.py as a standalone script (not importable as a module):

    1. Module docstring describing usage:
       """Fake adapter run — proves the full package wiring end-to-end.
       Creates a hand-crafted output directory (placeholder deliverables),
       writes metrics.json, and optionally invokes the LAB evaluator.
       Usage:
           uv run python scripts/fake_run.py --task antitrust-competition/analyze-antitrust-hsr-strategy
           uv run python scripts/fake_run.py --task antitrust-competition/analyze-antitrust-hsr-strategy --score
       """

    2. File header: from __future__ import annotations

    3. Imports: argparse, json, os, time, uuid from stdlib; html.escape from stdlib; pathlib.Path; zipfile.ZIP_DEFLATED, ZipFile; then from lab_harness_runner import all public symbols (Adapter, RunResult, TaskSpec, build_result_dir, read_task, score_run, write_metrics)

    4. Define reject_unsafe_relative_path(value: str, name: str) -> Path — exact copy from lab_probe.py lines 13-19 (local function; fake_run.py is standalone)

    5. Define write_minimal_docx(path: Path, lines: list[str]) -> None — exact copy from lab_probe.py lines 39-70

    6. Define _lab_path(override: Path | None = None) -> Path — same implementation as task_reader._lab_path: check override, then HARVEY_LAB_PATH env, then Path.home() / "Projects" / "harvey-labs"

    7. Define FakeAdapter class (no inheritance from Adapter — structural subtyping per D-07):
       - def run(self, task_spec: TaskSpec, output_dir: Path) -> RunResult:
         - start = time.monotonic()
         - For each filename in task_spec.expected_deliverables:
           - filepath = output_dir / filename
           - If filepath.suffix.lower() == ".docx": call write_minimal_docx(filepath, ["Placeholder", f"Task: {task_spec.task_id}"])
           - Else: filepath.write_text(f"Placeholder for {filename} — fake_run.py", encoding="utf-8")
         - return RunResult(run_id=task_spec.run_id, end_state="clean", wall_clock_seconds=time.monotonic() - start)

    8. Define main() -> int:
       - argparse with description=__doc__
       - --task (required)
       - --run-id (optional, default None → uuid4)
       - --lab-path (optional, default None → _lab_path())
       - --score (store_true)
       - --judge-model (default "claude-sonnet-4-6")
       - Validate: reject_unsafe_relative_path(args.task, "--task")
       - If args.run_id provided: reject_unsafe_relative_path(args.run_id, "--run-id")
       - run_id = args.run_id or str(uuid.uuid4())
       - lab_path = Path(args.lab_path).expanduser().resolve() if args.lab_path else _lab_path()
       - task_spec = read_task(lab_path=lab_path, task_id=args.task, run_id=run_id)
       - run_dir, output_dir = build_result_dir(lab_path=lab_path, run_id=run_id)
       - adapter = FakeAdapter()
       - result = adapter.run(task_spec=task_spec, output_dir=output_dir)
       - write_metrics(run_dir=run_dir, result=result)
       - Print: f"Run directory: {run_dir}"
       - Print: f"Run ID: {run_id}"
       - Print: f"Deliverables: {', '.join(task_spec.expected_deliverables)}"
       - If args.score: scores_path = score_run(lab_path=lab_path, run_id=run_id, task_id=args.task, expected_deliverables=task_spec.expected_deliverables, judge_model=args.judge_model); print(f"Scores: {scores_path}")
       - Else: print("Scoring skipped (pass --score to invoke evaluator)")
       - return 0

    9. Entry point: if __name__ == "__main__": raise SystemExit(main())

    10. Run `uv run black scripts/fake_run.py`
  </action>
  <verify>
    <automated>uv run python scripts/fake_run.py --task antitrust-competition/analyze-antitrust-hsr-strategy 2>&1 | grep -c "Run directory:"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/fake_run.py exists
    - `uv run python scripts/fake_run.py --task antitrust-competition/analyze-antitrust-hsr-strategy` exits 0
    - Output contains "Run directory:" and "Deliverables:"
    - After run, a directory exists under ~/Projects/harvey-labs/results/<uuid>/
    - ~/Projects/harvey-labs/results/<uuid>/metrics.json exists and is valid JSON
    - ~/Projects/harvey-labs/results/<uuid>/output/ contains the expected deliverable files
    - fake_run.py contains `from lab_harness_runner import` (imports from public package API, not internal modules)
    - fake_run.py contains `class FakeAdapter:` without `(Adapter)` in the class definition (structural subtyping)
    - `uv run black --check scripts/fake_run.py` exits 0
  </acceptance_criteria>
  <done>fake_run.py exercises the full package pipeline end-to-end. It creates a valid LAB run directory with placeholder deliverables and metrics.json. The Phase 2 exit criterion is met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --task CLI arg → filesystem path | User-supplied task_id becomes path component under lab_path |
| --run-id CLI arg → filesystem path | User-supplied run_id becomes path component under lab_path |
| --lab-path CLI arg → filesystem root | User-supplied lab path used as root for all file operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | --task and --run-id CLI args → path traversal | mitigate | reject_unsafe_relative_path applied to both --task and --run-id before any filesystem operation; raises ValueError for ".." or absolute paths |
| T-02-13 | Tampering | --lab-path CLI arg | accept | fake_run.py is a developer tool run explicitly by the user; --lab-path is user's own filesystem; expanduser().resolve() normalizes the path; no additional validation required |
| T-02-14 | Elevation of Privilege | --score flag triggers subprocess (evaluator) | accept | Score is opt-in; requires explicit --score flag; no auto-invocation; user controls when external API calls are made |
| T-02-15 | Information Disclosure | Run directory created under ~/Projects/harvey-labs/results/ | accept | Local-only developer tool; no PII or secrets in placeholder deliverables |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (stdlib only) |
</threat_model>

<verification>
- `uv run python scripts/fake_run.py --task antitrust-competition/analyze-antitrust-hsr-strategy` exits 0
- Run directory exists under harvey-labs/results/
- metrics.json exists and passes `python -c "import json; json.load(open('metrics.json'))"`
- Expected deliverable files exist in output/
- `uv run pytest tests/ -x -q` still passes (no regressions from fake_run.py creation)
- `uv run black --check scripts/fake_run.py` exits 0
</verification>

<success_criteria>
Phase 2 exit criterion is met: fake_run.py imports from lab_harness_runner, creates a valid LAB run directory with placeholder deliverables and metrics.json, and exits 0. A fake adapter satisfies the Adapter Protocol structurally. The full package wiring is proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/02-build-harness-neutral-package-core/02-05-SUMMARY.md` when done.
</output>
