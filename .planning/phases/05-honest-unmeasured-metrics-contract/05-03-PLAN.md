---
phase: 05-honest-unmeasured-metrics-contract
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - tests/test_aggregation.py
  - tests/test_docs.py
  - docs/adapter-guide.md
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03
user_setup: []

must_haves:
  truths:
    - "test_aggregation.py covers mixed measured + unmeasured rows: variance counts exclude unmeasured rows, `unmeasured_counts` is present, list-field `lengths` block skips null rows, and per-row `metrics_provided` boolean reflects any-null"
    - "test_aggregation.py's existing `summary['rows'] == rows` assertion is updated to compare with the annotated rows (with `metrics_provided` added)"
    - "docs/adapter-guide.md adds a one-paragraph addendum to 'Metrics And Status Semantics' noting that token / coverage fields are now nullable in `metrics.json` and that `null` means 'not measured', not 'zero'"
    - "docs/adapter-guide.md updates the 'RunResult' field list to show `documents_read_list` and `documents_skipped_list` as `list[str] | None` and notes that `None` is a valid unmeasured value"
    - "docs/adapter-guide.md's 'Results are whole agent-system outcomes' sentence at the end of 'Scoring And Report Preservation' is UNCHANGED (Phase 5 exit criterion — additive, not a rewrite of `end_state` semantics)"
  artifacts:
    - path: "tests/test_aggregation.py"
      provides: "Updated existing tests (rows == annotated rows) plus new tests for mixed measured/unmeasured, unmeasured_counts, list-field lengths, metrics_provided"
      contains: "test_build_summary_mixed_measured_and_unmeasured_rows"
    - path: "tests/test_docs.py"
      provides: "Doc test that the null-vs-zero wording is present in adapter-guide.md"
      contains: "test_adapter_guide_documents_null_vs_zero_distinction"
    - path: "docs/adapter-guide.md"
      provides: "Addendum to 'Metrics And Status Semantics' + 'RunResult' field list update"
      contains: "null means \"not measured\""
  key_links:
    - from: "tests/test_aggregation.py"
      to: "lab_harness_runner/aggregation.py::build_summary"
      via: "Direct function call; construct rows with mixed measured + unmeasured values, assert summary shape"
      pattern: "build_summary\\(\\[.*\\]\\)"
    - from: "docs/adapter-guide.md"
      to: "lab_harness_runner/metrics.py::write_metrics"
      via: "The doc addendum must match the runtime contract — null on disk means unmeasured, not zero"
      pattern: "null means"
---

<objective>
Add tests for the aggregation-layer null-vs-zero contract (CON-02 + CON-03), update existing tests in `test_aggregation.py` whose `summary["rows"] == rows` equality assertion no longer holds after Plan 02's `metrics_provided` annotation, and update `docs/adapter-guide.md` with the nullability addendum (CON-01). Also add a doc test that the new wording is present so the contract change is regression-protected.

Purpose: Closes the test gap for Phase 5 (D-17, D-18) and the doc gap (D-15, D-16). The "Results are whole agent-system outcomes" sentence is the Phase 5 exit-criterion invariant and must NOT be changed.

Output: Three files updated — `tests/test_aggregation.py` (new tests + existing-test update), `tests/test_docs.py` (new doc test), `docs/adapter-guide.md` (two addenda).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-honest-unmeasured-metrics-contract/05-CONTEXT.md
@.planning/phases/05-honest-unmeasured-metrics-contract/05-RESEARCH.md
@.planning/phases/05-honest-unmeasured-metrics-contract/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing test_aggregation.py assertions and add new tests for the null-vs-zero contract</name>
  <files>tests/test_aggregation.py</files>
  <read_first>
    - tests/test_aggregation.py
    - lab_harness_runner/aggregation.py (post-Phase-5 contract)
    - .planning/phases/05-honest-unmeasured-metrics-contract/05-CONTEXT.md (D-18 specifically)
    - .planning/phases/05-honest-unmeasured-metrics-contract/05-RESEARCH.md
  </read_first>
  <action>
Per D-18, modify `tests/test_aggregation.py` so the test file reflects the new aggregation contract:

1. UPDATE the test `test_build_summary_includes_rows_and_variance_fields` (lines 37-105): after Plan 02, `build_summary` annotates each row with a `metrics_provided` boolean. The current assertion `assert summary["rows"] == rows` (line 93) compares raw input rows to annotated output rows and will fail. Change it to construct the expected annotated rows inline:

   ```python
   expected_rows = []
   for row in rows:
       annotated = dict(row)
       annotated["metrics_provided"] = True  # all LAB metric fields are non-null
       expected_rows.append(annotated)
   assert summary["rows"] == expected_rows
   ```

   Also add an assertion that `summary["unmeasured_counts"]` is present and is the expected dict: `{"input_tokens": 0, "output_tokens": 0, "wall_clock_seconds": 0, "documents_read": 0, "total_vdr_files": 0, "documents_skipped": 0, "documents_read_list": 0, "documents_skipped_list": 0}` (all zeros because the two fixture rows set every LAB metric field). Add an assertion that the existing `variance` keys are still the same (`score`, `wall_clock_seconds`, `input_tokens`, `output_tokens`, `documents_read`, `total_vdr_files`).

2. UPDATE the test `test_write_batch_summary_is_metadata_only_under_results_batches` (lines 108-144): the assertion `assert payload["rows"] == [row]` (line 142) will also fail because of the `metrics_provided` annotation. Same fix: build an `expected_row = dict(row); expected_row["metrics_provided"] = True` and compare `payload["rows"] == [expected_row]`. The `missing_deliverables` is `[]` in the row fixture, and `metrics_provided` is `True` because all LAB metric fields are non-null.

3. ADD a new test `test_build_summary_mixed_measured_and_unmeasured_rows` per D-18: construct two rows where row 1 has `input_tokens=100, output_tokens=50, documents_read=3, total_vdr_files=4, wall_clock_seconds=12.0` (all measured) and row 2 has `input_tokens=None, output_tokens=None, documents_read=None, total_vdr_files=None, wall_clock_seconds=None` (all unmeasured). The other fields (batch_id, task_id, seed, adapter, run_id, run_dir, output_dir, metrics_path, scores_path, report_path, benchmark_status, raw_end_state, terminal_status_seen, expected_deliverables_present, missing_deliverables, score, all_pass) take the same values as in the existing test fixture. Build the summary and assert:
   - `summary["unmeasured_counts"]["input_tokens"] == 1`
   - `summary["unmeasured_counts"]["output_tokens"] == 1`
   - `summary["unmeasured_counts"]["wall_clock_seconds"] == 1`
   - `summary["unmeasured_counts"]["documents_read"] == 1`
   - `summary["unmeasured_counts"]["total_vdr_files"] == 1`
   - `summary["unmeasured_counts"]["documents_skipped"] == 0` (not in fixture but counted)
   - `summary["unmeasured_counts"]["documents_read_list"] == 0`
   - `summary["unmeasured_counts"]["documents_skipped_list"] == 0`
   - `summary["variance"]["input_tokens"]["count"] == 1` (only measured row)
   - `summary["variance"]["input_tokens"]["mean"] == 100`
   - `summary["variance"]["input_tokens"]["min"] == 100`
   - `summary["variance"]["input_tokens"]["max"] == 100`
   - `summary["variance"]["input_tokens"]["stdev"] == 0.0`
   - `summary["rows"][0]["metrics_provided"] is True`
   - `summary["rows"][1]["metrics_provided"] is False`

4. ADD a new test `test_build_summary_unmeasured_counts_zero_for_all_measured_rows` per D-18: construct two rows where every LAB metric field is set to a non-null value (a measured-only batch). Assert `summary["unmeasured_counts"]` is a dict with eight keys (the eight LAB metric fields) and every value is `0`. Assert both rows have `metrics_provided is True`.

5. ADD a new test `test_build_summary_list_field_lengths_skip_null_rows` per D-18: construct two rows where row 1 has `documents_read_list=["a.pdf", "b.pdf", "c.pdf"]` and `documents_skipped_list=["d.pdf"]`, and row 2 has `documents_read_list=None` and `documents_skipped_list=None` (the other LAB metric fields like `input_tokens` must be set to non-null values like `0` so they do not interfere). Assert:
   - `summary["variance"]["documents_read_list"]["lengths"]["count"] == 1`
   - `summary["variance"]["documents_read_list"]["lengths"]["mean"] == 3.0`
   - `summary["variance"]["documents_read_list"]["lengths"]["min"] == 3.0`
   - `summary["variance"]["documents_read_list"]["lengths"]["max"] == 3.0`
   - `summary["variance"]["documents_read_list"]["lengths"]["stdev"] == 0.0`
   - `summary["variance"]["documents_skipped_list"]["lengths"]["count"] == 1`
   - `summary["variance"]["documents_skipped_list"]["lengths"]["mean"] == 1.0`
   - `summary["rows"][0]["metrics_provided"] is True`
   - `summary["rows"][1]["metrics_provided"] is False` (because `documents_read_list=None` makes it False even though the count fields are set)

6. ADD a new test `test_build_summary_metrics_provided_false_when_any_field_null` per D-06/D-18: construct a row where ALL LAB metric fields except `input_tokens` are set, and `input_tokens=None`. Assert `summary["rows"][0]["metrics_provided"] is False`. Then construct a row where ALL LAB metric fields are non-null; assert `summary["rows"][0]["metrics_provided"] is True`.

7. KEEP unchanged: `test_variance_empty_values_has_count_only`, `test_variance_singleton_uses_zero_stdev`, `test_variance_multiple_values_uses_sample_stdev`. These test `summarize_variance` directly and the implementation is unchanged.

The result of these edits is a test file where every assertion is consistent with the new aggregation contract.
  </action>
  <verify>
    <automated>uv run --quiet python -m pytest tests/test_aggregation.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "test_build_summary_mixed_measured_and_unmeasured_rows\|test_build_summary_unmeasured_counts_zero_for_all_measured_rows\|test_build_summary_list_field_lengths_skip_null_rows\|test_build_summary_metrics_provided_false_when_any_field_null" tests/test_aggregation.py` returns one match each (four new tests added)
    - The two existing tests `test_build_summary_includes_rows_and_variance_fields` and `test_write_batch_summary_is_metadata_only_under_results_batches` no longer contain `assert summary["rows"] == rows` or `assert payload["rows"] == [row]`; they compare against an annotated-row copy instead
    - `uv run --quiet python -m pytest tests/test_aggregation.py -q` exits 0
    - `uv run --quiet python -m pytest tests/test_metrics.py tests/test_aggregation.py tests/test_run_benchmark.py -q` exits 0 (full module test suite green)
  </acceptance_criteria>
  <done>test_aggregation.py: existing tests updated to expect `metrics_provided` annotation; four new tests cover mixed measured/unmeasured, unmeasured_counts zero case, list-field lengths skipping null rows, and per-row boolean semantics. The aggregation test module passes under the new contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/adapter-guide.md and add a doc test that the null-vs-zero wording is present</name>
  <files>docs/adapter-guide.md, tests/test_docs.py</files>
  <read_first>
    - docs/adapter-guide.md
    - tests/test_docs.py
    - .planning/phases/05-honest-unmeasured-metrics-contract/05-CONTEXT.md (D-15, D-16 specifically)
    - .planning/phases/05-honest-unmeasured-metrics-contract/05-RESEARCH.md (Pitfall 5 specifically)
  </read_first>
  <action>
Per D-15, D-16, and Pitfall 5: update `docs/adapter-guide.md` and add a regression doc test.

1. In `docs/adapter-guide.md`, update the "RunResult" field list (the bullet at lines 33-36 that currently reads:

   ```
   - `input_tokens`, `output_tokens`: optional token counts.
   - `documents_read`, `total_vdr_files`, `documents_skipped`: optional document
     coverage counts.
   - `documents_read_list`, `documents_skipped_list`: optional document lists.
   ```

   ) to reflect the nullability change. The new shape is:

   ```
   - `input_tokens`, `output_tokens`: optional token counts; `None` means
     unmeasured, `0` means measured zero.
   - `documents_read`, `total_vdr_files`, `documents_skipped`: optional document
     coverage counts; `None` means unmeasured, `0` means measured zero.
   - `documents_read_list`, `documents_skipped_list`: optional document lists
     of type `list[str] | None`; `None` means unmeasured, `[]` means measured
     zero.
   ```

2. In `docs/adapter-guide.md`, append a new paragraph to the "Metrics And Status Semantics" section (immediately after the existing paragraph that ends with "...the timeout remains diagnostic evidence and must not be rewritten to `clean` inside `RunResult`."). The new paragraph reads:

   ```
   `metrics.json` distinguishes "adapter did not measure" from "adapter measured
   zero". The LAB-compatible token and coverage fields (`input_tokens`,
   `output_tokens`, `wall_clock_seconds`, `documents_read`, `total_vdr_files`,
   `documents_skipped`, `documents_read_list`, `documents_skipped_list`) are
   nullable on disk: a `null` value means the adapter did not measure that
   field, not that the measurement was zero. An explicit `0` is preserved
   verbatim. Adapter authors should leave a field unset (or set it to `None`)
   when the harness did not surface a measurement; downstream consumers and
   `build_summary` use the `null` value to skip the field in mean / sum /
   variance computations and to record an unmeasured-row count.
   ```

3. CRITICAL: Do NOT modify the "Results are whole agent-system outcomes" sentence at the end of the "Scoring And Report Preservation" section (lines 151-153). This is the Phase 5 exit-criterion invariant — the contract change is additive, not a rewrite of `end_state` semantics. Also do not modify the "Add, don't rewrite" `end_state` discussion or the "Raw adapter status and benchmark-facing status" section.

4. In `tests/test_docs.py`, ADD a new test `test_adapter_guide_documents_null_vs_zero_distinction` after the existing `test_adapter_guide_explains_timeout_with_valid_deliverables`. The test reads the guide text and asserts these substrings are all present (each on its own line or as part of the same paragraph):

   - `"null"` appears (the substring `null`)
   - `"unmeasured"` appears (the substring `unmeasured`)
   - `"measured zero"` appears (the substring `measured zero`)

   Also assert that the RunResult field list section contains `list[str] | None` (the substring `list[str] | None`) AND that the guide does NOT contain the old phrase "optional document lists." as a standalone (without nullability context) — assert the phrase `optional document lists` is NOT followed by just a period and a newline; assert the new phrase `optional document lists of type` IS present.

5. KEEP unchanged: the existing three tests in `tests/test_docs.py` (`test_adapter_guide_documents_required_contract_terms`, `test_adapter_guide_explains_timeout_with_valid_deliverables`, `test_adapter_guide_mentions_second_adapter_without_implementation`, `test_public_docs_do_not_reference_local_user_paths`). They still pass against the updated doc — the required contract terms list (run(task_spec, output_dir) -> RunResult, TaskSpec, RunResult, benchmark_status, raw_end_state, terminal_status_seen, completion_signal, expected_deliverables_present, missing_deliverables, metrics.json, summary.json, results/<run-id>/) are all still present.
  </action>
  <verify>
    <automated>uv run --quiet python -m pytest tests/test_docs.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "list\[str\] | None" docs/adapter-guide.md` returns at least one match (the new field-list wording)
    - `grep -n "unmeasured" docs/adapter-guide.md` returns at least one match (the new addendum)
    - `grep -n "measured zero" docs/adapter-guide.md` returns at least one match (the new addendum)
    - `grep -n "Results are whole agent-system outcomes" docs/adapter-guide.md` returns exactly one match (the invariant sentence is unchanged)
    - `grep -n "test_adapter_guide_documents_null_vs_zero_distinction" tests/test_docs.py` returns one match (new test added)
    - `uv run --quiet python -m pytest tests/test_docs.py -q` exits 0 (all doc tests pass)
    - `uv run --quiet python -m pytest -q` exits 0 (full test suite green)
  </acceptance_criteria>
  <done>docs/adapter-guide.md: RunResult field list updated to show `list[str] | None`; "Metrics And Status Semantics" addendum explains the null-vs-zero distinction. The "Results are whole agent-system outcomes" sentence is unchanged. tests/test_docs.py has a new doc test that the new wording is present and the test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → aggregation.py / metrics.py | Tests are trusted (in-tree) and exercise the contract |
| docs/adapter-guide.md → adapter authors | The doc is a contract surface; the new wording must be accurate and not introduce confusion |
| tests/test_docs.py → docs/adapter-guide.md | Doc tests check that the new wording is present so the contract change is regression-protected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | The new "null means unmeasured" doc wording could be misinterpreted by an adapter author who has not yet updated their code | accept | The doc change is additive and the contract is what it is. The wording is plain English. |
| T-05-06 | Repudiation | A doc change could remove the Phase 5 exit-criterion invariant "Results are whole agent-system outcomes" | mitigate | The acceptance criteria explicitly assert that the sentence is still present (one-match grep) and the doc test (test_docs.py) does not depend on its removal. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. Stdlib + pytest only. |
</threat_model>

<verification>
- `uv run --quiet python -m pytest tests/test_aggregation.py -q` is green.
- `uv run --quiet python -m pytest tests/test_docs.py -q` is green.
- `uv run --quiet python -m pytest -q` (full suite) is green.
- `docs/adapter-guide.md` still contains "Results are whole agent-system outcomes" as a complete sentence.
- `docs/adapter-guide.md` lists `documents_read_list, documents_skipped_list: optional document lists of type list[str] | None` (the new wording).
- `docs/adapter-guide.md`'s "Metrics And Status Semantics" section contains a paragraph explaining that `null` in `metrics.json` means "unmeasured", not "zero".
- The RunResult field list reflects the `list[str] | None` typing and explains the `None` vs `[]` distinction.
</verification>

<success_criteria>
- `tests/test_aggregation.py` has the four new tests and the two updated tests; the aggregation test module passes.
- `tests/test_docs.py` has the new `test_adapter_guide_documents_null_vs_zero_distinction` test; all doc tests pass.
- `docs/adapter-guide.md` has the updated RunResult field list and the new "Metrics And Status Semantics" addendum.
- The "Results are whole agent-system outcomes" sentence in `docs/adapter-guide.md` is unchanged (Phase 5 exit criterion).
- The full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/05-honest-unmeasured-metrics-contract/05-03-SUMMARY.md` when done.
</output>
